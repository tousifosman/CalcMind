# 2026-08-08

## PR preview deploy: `workflow_dispatch` `number` input serializes as a float

### Session log

1. Follow-up to 2026-08-06's PR-preview work (PR #130, merged). Two problems
   surfaced once it actually ran against real GitHub infrastructure:
   - The live Pages site stayed on the *old* `actions/deploy-pages` build from
     before that PR, because Settings → Pages → Source was still "GitHub
     Actions" rather than "Deploy from a branch" — confirmed by comparing the
     bundle hash and `last-modified` header the live site served against what
     was actually on the `gh-pages` branch tip. Talked the user through the
     Settings UI to flip it by hand (mobile Safari's tab row hides Settings
     off-screen — worth remembering if this comes up again).
   - Manually running `deploy-pr-preview.yml` against PR #133 failed at
     checkout: `fatal: couldn't find remote ref refs/pull/133.0/head`.
2. The log's own printed `with:` block showed the ref as
   `refs/pull/133.0/head` — not `133`. `pr_number` was declared
   `type: number` in `workflow_dispatch.inputs`; GitHub stores that as a
   float internally, so every expression interpolation of it
   (`${{ inputs.pr_number }}`) renders `"133.0"`, not `"133"`. That string
   then flows untouched through the `determine` job's output into the
   `refs/pull/<n>/head` ref, which doesn't exist with a `.0` in it.
3. Fix: changed `pr_number`'s input type from `number` to `string`. Nothing
   in the workflow ever does arithmetic on it — it's only ever concatenated
   into ref paths, a `destination_dir`, and a comment body — so there was no
   reason to have chosen `number` beyond it "being a number" conceptually.

### Findings

**`workflow_dispatch` inputs declared `type: number` serialize into
expressions with a trailing `.0`.** GitHub stores the value as a float
regardless of what the user types, and `${{ inputs.foo }}` renders that float
directly — `133` becomes `"133.0"` wherever it's interpolated into a string.
Misleading symptom: the input *looks* right everywhere in the Actions UI (the
run form just shows `133`), and the workflow YAML looks correct too — the
`.0` only appears once you read the *expanded* `with:` block in a run's log,
which is easy not to check when a checkout step fails with what looks like a
generic "ref not found." Fix/avoidance: declare any `workflow_dispatch`
input as `type: string` unless the workflow actually does numeric
comparison or arithmetic on it — text-only usage (refs, paths, comment
bodies, as here) has nothing to gain from `type: number` and everything to
lose from this quirk.

### Knowledge revisions

**1. "Both new/changed workflow YAML files parse" (2026-08-06's closing
checklist) was necessary but not sufficient to call the PR-preview workflow
verified.**
*Previously believed:* `yaml.safe_load` succeeding plus `npm run build:web`
still working was enough confidence to merge, with the real end-to-end run
explicitly flagged as an open thread to check later.
*Now known:* that flag was the right call — the first real
`workflow_dispatch` run did fail, on something YAML syntax-checking can't
catch (GitHub's own input-type serialization behavior). Confirms the
existing standing lesson (`docs/journal/GUIDELINES.md`, "verify before
claiming"): a workflow that parses is not a workflow that runs correctly,
same shape as "green `tsc` is not a working app."
*What established it:* this session's failed run against PR #133.

### Open threads

- Still need the repo admin to actually flip Settings → Pages → Source to
  "Deploy from a branch" (`gh-pages`, root) — talked through as of this
  entry, not yet confirmed done.
- `deploy-pr-preview.yml`'s `deploy` path (checkout → build → publish →
  comment) has still never completed successfully end-to-end — the one real
  attempt failed at the first step. Re-verify against a real open PR once
  this fix lands and the Pages source is switched.
- Carried over from 2026-08-06: `keep_files: true` root-level bundle
  accumulation on `gh-pages` is unaddressed.

### Closing checklist

- [x] Journal entry written (revision on "parses ⇒ verified enough")
- [x] Workflow YAML re-parses (`yaml.safe_load`)
- [ ] Not yet re-run against a real PR — see open thread
- [x] `npx tsc --noEmit` green (sanity check only — no app code touched by
      this entry, lint/test/build:web not re-run)

---

## P7.8 follow-up — hide keypad during clear confirm

### Session log

1. Feedback on the Clear all confirm: while "Clear the whole canvas?" is up,
   the keypad keys were still filling the bottom half (annotated: hide keys,
   keep the dialog).
2. When `clearConfirmVisible`, `Keypad` now renders only the confirm chrome —
   mode strip and digit/operator keys stay unmounted. Cancel or Clear dismisses
   the confirm and restores the keypad.
3. §8.5 and P7.8 acceptance criteria updated to match. Unit coverage for
   hide-on-confirm and restore-on-cancel.

### Findings

None.

### Knowledge revisions

None — UI polish on an already-shipped confirm gate.

### Open threads

- Carried over from earlier on this day: Pages source switch; PR-preview
  end-to-end re-verify; `keep_files` accumulation.

### Closing checklist

- [x] Confirm hides keypad chrome; Cancel/Clear restores it
- [x] §8.5 + P7.8 criterion updated
- [x] Live browser check of confirm-only bottom UI (screenshots
      `/opt/cursor/artifacts/screenshots/p78-confirm-*.png`)
- [x] `npx tsc --noEmit`, `npm run lint`, `npm test` (697), `npm run build:web` green

---

## Suppress OS soft keyboard on number edit

### Session log

1. Bug report with screenshot: editing a number showed the iOS system numeric
   keyboard stacked on the custom keypad. Architecture §8.5 already treats the
   app keypad as the soft-input surface; the OS keyboard is redundant.
2. Root cause was the open thread left by P2.6 (`docs/journal/2026-08-03.md`):
   `NumberNode`'s edit `TextInput` used the platform default for
   `showSoftInputOnFocus`, deliberately, until P2.8 wired keypad presses into
   the same edit target. P2.8 (and P7.2) did that wiring; the prop was never
   added.
3. Set `showSoftInputOnFocus={false}` on the number-edit `TextInput` only.
   Label and value-slider bound inputs keep the system keyboard. Unit test
   asserts the prop. RN-web maps the same prop to
   `virtualkeyboardpolicy="manual"` — no separate `inputMode` needed for the
   web path this session checked.

### Findings

**`showSoftInputOnFocus={false}` is the one-line native fix for "custom keypad
+ OS keyboard both visible," and RN-web already forwards it.** Mechanism:
native RN suppresses the IME while leaving the field focusable (caret /
hardware keys still work); react-native-web sets
`virtualkeyboardpolicy="manual"` on the host `<input>`. Misleading symptom:
the custom keypad looks correct and receives presses, so it is easy to assume
the OS keyboard is a separate bug in focus-steal prevention (`Keypad`'s
`onMouseDown` preventDefault) rather than the edit `TextInput` inviting the
IME by default. The 2026-08-03 deferral already named the fix; the gap was
simply never closing it after P2.8.

### Knowledge revisions

**2. Soft-keyboard suppression was deferred until keypad wiring, then never
applied.**
*Previously believed (as an open thread, not a standing design):* leave
`showSoftInputOnFocus` unset on `NumberNode` until P2.8, then decide.
*Now known:* with keypad + hardware dispatch in place, the answer is
`showSoftInputOnFocus={false}` on the number editor; label/slider inputs stay
default so they can use the OS keyboard.
*What established it:* the deferred note in `2026-08-03`, the live iOS
screenshot of both keyboards, and the fact that P2.8/`dispatchEditorCommand`
already update `raw` without needing the IME.

### Open threads

- Carried over from earlier on this day: Pages source switch; PR-preview
  end-to-end re-verify; `keep_files` accumulation.
- Native iOS/Android soft-keyboard absence was not re-checked on a device in
  this environment — only the prop + unit test + web build. Confirm on a real
  device that the OS keyboard stays down while the custom keypad works.

### Closing checklist

- [x] `showSoftInputOnFocus={false}` on number-edit `TextInput`
- [x] Label / slider inputs untouched
- [x] Unit assertion on the prop
- [x] `npx tsc --noEmit`, `npm run lint`, `npm test` (697), `npm run build:web` green
- [x] Web smoke: custom keypad types into focused number; host `<input>` has
      `virtualkeyboardpolicy="manual"` (screenshots under
      `/opt/cursor/artifacts/screenshots/suppress-keyboard-*.webp`)
- [ ] Device confirm of no OS keyboard — see open thread

---

## Soft-keyboard suppression needed `inputMode="none"` on web

### Session log

1. After PR #136 merged (`showSoftInputOnFocus={false}`), the reporter still
   saw the OS virtual keyboard while editing a number — same stacking with
   the custom keypad.
2. Re-read RN-web's `TextInput`: `showSoftInputOnFocus={false}` only sets
   `virtualkeyboardpolicy="manual"`. Separately, `keyboardType="numeric"`
   still emits `inputMode="numeric"`. Safari (and other mobile browsers)
   honour `inputMode` for which soft keyboard to raise and largely ignore
   `virtualkeyboardpolicy`, so the numeric pad kept appearing.
3. Fix: set `inputMode="none"` on the number-edit `TextInput` and drop
   `keyboardType="numeric"` so it cannot reintroduce a numeric inputMode.
   Keep `showSoftInputOnFocus={false}` for native. Label / slider inputs
   untouched.

### Findings

**On react-native-web, `showSoftInputOnFocus={false}` is not a soft-keyboard
suppressor by itself.** It only sets `virtualkeyboardpolicy="manual"`. The
attribute browsers actually use to keep the IME down is `inputMode="none"`.
Worse, leaving `keyboardType="numeric"` sets `inputMode="numeric"`, which
actively invites the OS numeric pad on iOS Safari. Misleading symptom: desktop
Chrome never shows a soft keyboard, so a desktop web smoke that checks
`virtualkeyboardpolicy="manual"` looks green while mobile Safari still pops
the pad. The RN-web issue thread (#2421) and the Expensify patch both pointed
at `inputMode='none'` as the real mapping; upstream RN-web never tied
`showSoftInputOnFocus` to that.

### Knowledge revisions

**3. `showSoftInputOnFocus={false}` alone closes the native gap, not the
mobile-web one.**
*Previously believed (this day's earlier entry, revision 2 / PR #136):*
`showSoftInputOnFocus={false}` is the one-line fix cross-platform, and
RN-web's `virtualkeyboardpolicy="manual"` mapping means no separate
`inputMode` is needed.
*Now known:* keep `showSoftInputOnFocus={false}` for native, but mobile web
needs `inputMode="none"` and must not set `keyboardType="numeric"`. Desktop
web verification cannot prove soft-keyboard absence.
*What established it:* reporter still seeing the OS keyboard after #136;
RN-web source mapping of the two props; `inputMode` precedence over
`keyboardType` in that same file.
*Standing lesson:* a soft-keyboard claim needs a touch/mobile surface, or at
least a DOM attribute check for `inputMode="none"` — not only
`virtualkeyboardpolicy`.

### Open threads

- Carried over: Pages source switch; PR-preview end-to-end; `keep_files`;
  native device confirm of no OS keyboard (now also the mobile-Safari confirm
  for `inputMode="none"`).

### Closing checklist

- [x] `inputMode="none"` + drop `keyboardType` on number-edit `TextInput`
- [x] Keep `showSoftInputOnFocus={false}`
- [x] Unit assertions updated
- [x] `npx tsc --noEmit`, `npm run lint`, `npm test` (702), `npm run build:web` green
- [x] Headless Chromium (mobile viewport): focused number input has
      `inputMode="none"`, `type="text"`, `virtualkeyboardpolicy="manual"`
- [ ] Mobile Safari / device confirm — see open thread

---

## Keypad: merge `(` / `)` into one `()` key

### Session log

1. User annotated the live keypad: circle the separate `(` and `)` keys, ask to
   merge them into a single `()` button.
2. Replaced the grouping-row pair with one `()` key that reports
   `{ region: 'paren' }` (no `side`). `dispatchEditorCommand` resolves open vs
   close via `resolveParenSide`: prefer `)` when the chain through the selection
   has unmatched opens and a close is grammatical; otherwise `(`. Hardware
   `(`/`)` still send an explicit side.
3. Kept the empty-placeholder-after-operator special case (P4 regression:
   `2 × (3 + 4) =` → 14) by resolving side *before* discarding the placeholder.
4. §8.5 table + behaviour bullet updated; Keypad / keymap tests cover the
   merged key and smart side picking.

### Findings

None — pure UI consolidation on existing paren dispatch.

### Knowledge revisions

None. §1.3 still describes Tydlig's separate `(` `)` keys; §8.5 is our keypad
and now documents the merged `()` key. That divergence from the reference is
intentional (user request), not an accidental drift.

### Open threads

- Carried over: Pages source switch; PR-preview end-to-end re-verify;
  `keep_files` accumulation.

### Closing checklist

- [x] Live browser check that the keypad shows one `()` and that
      `2 × () 3 + 4 () =` evaluates to 14 (screenshots
      `/opt/cursor/artifacts/screenshots/paren-merged-*.webp`)
- [x] `npx tsc --noEmit`, `npm run lint`, `npm test` (702), `npm run build:web` green

---

## Selection focus ring was painted under flush chain neighbours

### Session log

1. User asked to visually show the cell that currently has focus, calling out
   that a read-only result still holds focus because typing an operator there
   starts §8.7 continuation.
2. P7.2 already drew an outset `selectionFocusColor` ring on `Cell` for every
   selected / group-selected node, including results. Live check showed the
   DOM node present, but mid-chain operators looked unfocused and results only
   showed a partial edge.
3. Root cause: idle positioned nodes all return `zIndex: 0` from
   `useNodeDrag`'s animated style, and chain members sit flush, so later
   siblings paint over the outset ring. Misleading symptom: the ring "exists"
   in DevTools and unit tests on a lone `OperatorNode` pass, while a real
   formula chain hides it.
4. Fix (PR #140): elevate the selected wrapper to `SELECTED_NODE_Z_INDEX` (10)
   while idle; whiten/thicken the ring (`#FFFFFF`, 3px / 4px outset); cover
   result + stacking in `SelectionFocus.test.tsx`; note the stacking rule in
   §8.6.
5. Live Chromium: mid-chain `÷` and result `2.4` both show a full surround;
   continuation from a focused result still works. Screenshots under
   `/opt/cursor/artifacts/screenshots/final_*_ring.webp`.

### Findings

**An outset focus ring on a flush chain member is invisible unless the
selected wrapper stacks above its neighbours.** Mechanism: NodeLayer places
every member at the same idle `zIndex: 0`; document order then wins, so the
next cell covers the 3–4px of ring that sticks out into its box. Unit tests
that mount a single node never exercise that. Misleading symptom: DevTools
shows `visibility: visible` and a light border colour, which looks like a
contrast problem rather than a paint-order problem.

### Knowledge revisions

**4. "Focus is always visible" (P7.2) was true in isolation, false in a chain.**
*Previously believed:* painting an outset ring on `Cell` with
`selectionFocusColor` satisfied P7.2's "Focus is always visible" for every
kind, including results.
*Now known:* the ring must also win the sibling paint order — elevate the
selected node's wrapper above idle neighbours (and keep it below connectors /
live drag). Colour/thickness alone cannot fix a ring that is under the next
cell.
*What established it:* live Chromium on `12 ÷ 5 = 2.4` with the ring in the
DOM but not surrounding mid-chain `÷`; z-index 10 after the fix made the full
ring visible.
*Standing lesson:* an outset decoration on chain members needs an explicit
stacking rule, or a test that mounts a multi-member chain and asserts the
selected wrapper's z-index — a single-node ring assertion is not enough.

### Open threads

- Carried over: Pages source switch; PR-preview end-to-end; `keep_files`;
  mobile Safari soft-keyboard confirm.
- `snapSearch.profile.test.ts`'s hash-vs-linear timing assert is load-sensitive
  (failed once under a full suite at p95=4 vs linear 1, passed alone) — not
  touched here; revisit if CI flakes.

### Closing checklist

- [x] Selected idle z-index elevated; ring whitened/thickened
- [x] Result + stacking unit tests
- [x] §8.6 updated
- [x] Live browser: ring surrounds mid-chain op and result; continuation works
- [x] `npx tsc --noEmit`, `npm run lint`, `npm test` (703 + flaky profile
      re-run green), `npm run build:web` green

---

## Select group highlight stuck after tapping elsewhere

### Session log

1. User screenshot from the live site: after Select group, the whole
   `12 + 5 = 17` chain stayed ringed even while interacting with other cells.
2. Cause: `selectGroup` wrote `groupSelectedIds`, and `useNodeSelected` treats
   membership as focused, but `selectNode` / `editNumberNode` / `deselectNode`
   never called `clearGroupSelected`. Tap-elsewhere updated only
   `selectedNodeId`, so every prior group member kept its ring.
3. Clear the set on those three paths, plus `editNodeLabel` and the
   backspace-delete path that was nulling selection via bare uiStore setters.
   §8.6 notes the contract. Unit test covers the three commands.
4. Live: Select group → all five cells ringed; tap `+` → exactly one
   `-selection-focus` in the DOM (DevTools "1 of 1"). Screenshots
   `a6f61.webp` / `23008.webp` / `2f5d3.webp`.

### Findings

**`clearGroupSelected` existed but nothing in the selection command layer
called it on single-node moves.** Misleading symptom: Select group "worked"
(rings appeared, move-chain still used the set), so the sticky highlight looked
like a paint/z-index bug rather than a missing clear on the happy path that
already owned selection changes.

### Knowledge revisions

**5. Group highlight is not self-clearing via primary selection changes.**
*Previously believed (implicitly, by shipping `clearGroupSelected` unused on
the select/edit/deselect path):* replacing `selectedNodeId` was enough for the
user to "move on" from a group.
*Now known:* `useNodeSelected` ORs in `groupSelectedIds`, so the group set
must be cleared whenever selection becomes a single node or null — otherwise
every former member keeps the focus ring forever.
*What established it:* user screenshot of a stuck group outline; code search
showing `clearGroupSelected` only defined, never called from selection
commands; fix verified live with DevTools count of `-selection-focus`.

### Open threads

- Carried over unchanged from the prior entry on this day.

### Closing checklist

- [x] `clearGroupSelected` on select / edit / deselect / label-edit / backspace-delete
- [x] Unit test + §8.6 note
- [x] Live Select-group → tap-elsewhere clears group rings
- [x] `npx tsc --noEmit`, `npm run lint`, `npm test` (705), `npm run build:web` green

---

## Pressing `=` selects the result

### Session log

1. User: after `=`, focus should move to the result (so the next operator can
   continue without an extra tap).
2. `dispatchEditorCommand` was doing `selectNode(appendEqualsNode(...))`, which
   leaves focus on the `=` glyph. Result creation happens inside finalize /
   recompute, after the equals id is returned.
3. After append, look up the chain's result member and select that; fall back to
   `=` only when the chain did not evaluate. §8.5 bullet + keymap tests updated
   so continuation exercises the post-`=` selection without a manual
   `selectNode`.
4. Live: DevTools shows `result-node-*-selection-focus` after `=`; pressing an
   operator immediately starts §8.7 continuation.

### Findings

None beyond the selection target change itself.

### Knowledge revisions

None — this matches §8.7's "with a result selected" premise; the gap was that
`=` never put selection there.

### Open threads

- Carried over unchanged.

### Closing checklist

- [x] `=` selects result when present
- [x] Unit + live continuation-without-retap
- [x] §8.5 updated

---

## Selected cells dropped the inset identity ring

### Session log

1. User: focused number cells showed a blue border *inside* the cell as well
   as the outer focus indicator; outer alone is enough.
2. Cause: not a browser outline — the inset identity ring (`#2F6BFF` first
   swatch) stacked under the outset selection ring whenever an
   identity-bearing cell was selected.
3. `Cell` now skips the identity ring while `selected`; caption colour and
   connectors still carry hue (§11.1). §8.6 note + SelectionFocus test.

### Findings

**Double chrome on selected identity cells reads as a second focus ring.**
Misleading symptom: the inner border is identity hue, so it looks like a
Safari input outline rather than link colour — especially on the first
palette blue.

### Knowledge revisions

None beyond the selection-chrome precedence choice.

### Open threads

- Carried over unchanged.

### Closing checklist

- [x] Identity ring hidden while selected
- [x] Live + unit: selection-focus present, identity-ring absent

---

## Heroicons via `react-native-heroicons`

### Session log

1. User asked to add [Heroicons](https://github.com/tailwindlabs/heroicons) to the
   project. Official `@heroicons/react` emits DOM `<svg>` / `<path>` and would
   break the native target; the project already has `react-native-svg` for
   connectors and the result texture.
2. Added `react-native-heroicons@^4.0.0` (MIT) — same Tailwind Labs icon set,
   packaged as `react-native-svg` components. Recorded in §4 and §11.3.
3. First app use: keypad mode-strip dismiss (`⌄` text) → outline
   `ChevronDownIcon`, with `ModeKey` gaining an optional `icon` slot and keeping
   `label` as the accessibility name.
4. Smoke test mounts a deep-imported outline icon through the existing
   `react-native-svg` Jest mock. Live web check confirmed the dismiss control
   is an SVG with Heroicons stroke attributes.

### Findings

**`react-native-heroicons` style barrels pull the whole set into the webpack
bundle.** `import { ChevronDownIcon } from 'react-native-heroicons/outline'`
grew production `dist/` by ~340 KiB versus
`import ChevronDownIcon from 'react-native-heroicons/outline/ChevronDownIcon'`.
The package marks `sideEffects: false`, but the CJS/ESM barrels still eagerly
require every icon file; named-export tree-shaking did not drop the unused ones
in this Webpack 5 production build. Prefer deep imports
(`…/outline/IconName`). Misleading symptom: a single small icon "shouldn't"
move the bundle, and the README's barrel examples look like the happy path.

The package ships precompiled `createElement` JS, so it does **not** need to be
added to `webpack.config.js`'s Babel `include` list the way `react-native-svg`
does.

### Knowledge revisions

**1. Official `@heroicons/react` is the wrong package for this dual-target app.**
*Previously believed:* (implicitly, from the upstream README) install
`@heroicons/react` for any React project that wants Heroicons.
*Now known:* use `react-native-heroicons` here — it renders through
`react-native-svg` on native and web. `@heroicons/react` is DOM-only.
*What established it:* `@heroicons/react` components call
`React.createElement("svg" | "path", …)`; RN has no host for those tags.
`react-native-heroicons` maps the same paths onto `react-native-svg`'s `Svg` /
`Path`.
*Standing lesson:* for icon (and SVG) libraries, check whether the React
package targets the DOM or `react-native-svg` before adding it to a bare-RN +
`react-native-web` app.

### Open threads

- Carried over: Pages source switch; PR-preview end-to-end re-verify;
  `keep_files` accumulation; mobile Safari confirm for `inputMode=none`.
- Whether more mode-strip / future-toolbar chrome should move to Heroicons
  (Clear all, Documents, etc.) — only dismiss was converted as the first use.

### Closing checklist

- [x] `react-native-heroicons` in `package.json`; §4 + §11.3 updated
- [x] Deep-import smoke test + keypad dismiss uses `ChevronDownIcon`
- [x] Live web: dismiss control is SVG chevron (screenshots
      `/opt/cursor/artifacts/screenshots/heroicons-*.webp`)
- [x] `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build:web` green

---

## Canvas context menu: `Select all`

### Session log

1. User pointed at the empty-canvas long-press menu (`Add number` / `Paste` /
   `Add graph`) and asked for a `Select all` option there.
2. Wired `selectAll` into that menu: puts every node into the same ephemeral
   `groupSelectedIds` set that `Select group` uses, keeps the current keypad
   target when it still exists, discards an abandoned empty number first, and
   stays disabled on an empty canvas. §8.6 updated to list the item.

### Findings

None beyond the placement decision below.

### Knowledge revisions

**6. `Select all` lives on the empty-canvas menu, not the node menu.**
*Previously believed (from §1.2's Tydlig observation only):* select-all was a
number-menu affordance ("select all numbers on the canvas"), and §8.6 did not
list it at all.
*Now known:* CalcMind exposes `Select all` on the empty-canvas long-press menu,
selecting every node (not only numbers) via the existing group-selection set.
The node menu keeps `Select group` for the local chain.
*What established it:* explicit user request against that menu; reuse of
`groupSelectedIds` already drives the focus ring and move-chain route.

### Open threads

- Carried over: Pages source switch; PR-preview end-to-end; `keep_files`;
  mobile-Safari / device confirm of soft-keyboard suppression.
- Escape / tap-empty clearing `groupSelectedIds`: closed later the same day
  by main's sticky Select-group fix (revision 5), merged into this branch.

### Closing checklist

- [x] Live: long-press empty canvas → `Select all` → every node shows the
      selection focus ring (screenshots under
      `/opt/cursor/artifacts/screenshots/select-all-*.webp`)
- [x] Empty canvas: `Select all` greyed out
- [x] `npx tsc --noEmit`, `npm run lint`, `npm test` (related suites + flaky
      P7.6 profile re-run green), `npm run build:web`

---

## Select all drag must move every selected unit

### Session log

1. User reported that after Select all, dragging still moved only the dragged
   cell / its chain — other selected cells stayed put.
2. Root cause: Select all reused `groupSelectedIds`, and that flag only forced
   the existing single-chain `moveChain` path. Free nodes in the group were
   ignored (`groupSelected` required `chainId !== null`), and a second chain
   never received the shared Reanimated translate.
3. Added `moveSelection`: resolve the group into chain + free-node units; when
   there is more than one unit, mid-drag followers use `selectionDrag*` shared
   values and release commits every unit in one undo entry. Single-chain
   `Select group` still uses MovingChain.

### Findings

**`groupSelectedIds` is not automatically a multi-move selection.** It was
built for `Select group` → one chain → `moveChain`. Putting every node id in
that set (Select all) lights the focus ring but does not define a drag
commit that spans units. Misleading symptom: selection chrome looks correct,
so the drag looks like a gesture bug rather than a missing mode.

### Knowledge revisions

**7. Select all needs its own drag mode, not just the Select group flag.**
*Previously believed (this day's earlier entry, revision 6):* reusing
`groupSelectedIds` was enough because it "already drives the focus ring and
move-chain route."
*Now known:* focus ring yes; multi-unit translate no — that requires
`moveSelection` (shared follower transforms + one `moveSelection` command).
*What established it:* user report that Select-all drag left other cells
behind; confirmed against `useNodeDrag`'s single `movingChainId` path.

### Open threads

- Carried over: Pages source switch; PR-preview end-to-end; `keep_files`;
  soft-keyboard device confirm.
- Group-highlight clear on tap/Escape: closed by merging main's sticky
  Select-group fix (`clearGroupSelected` on select/edit/deselect).

### Closing checklist

- [x] Live (Playwright against `npm run web`): Select all → drag one free
      node → both free nodes moved by the same delta (203, 174); single-node
      drag still works as control. Screenshots
      `/opt/cursor/artifacts/screenshots/select-all-drag-*.png`
- [x] `npx tsc --noEmit`, `npm run lint`, targeted jest suites,
      `npm run build:web`

---

## Select-all highlight now clears after merging main's group clear

### Session log

1. User: after Select all, tapping empty canvas or a single cell left every
   node ringed.
2. Pulled `origin/main` into this branch. Main already cleared
   `groupSelectedIds` from `selectNode` / `editNumberNode` / `deselectNode`
   (and label-edit / backspace-delete) for sticky Select-group — the same
   paths empty-canvas and single-cell taps use. Conflict-resolved merge;
   no extra clear logic needed on the Select-all side.

### Findings

None beyond confirming Select all shares the Select-group clear contract.

### Knowledge revisions

None — revision 5 already recorded the clear rule; Select all was only
missing that commit on this branch.

### Open threads

- Carried over: Pages source switch; PR-preview end-to-end; `keep_files`;
  soft-keyboard device confirm.

### Closing checklist

- [ ] Live: Select all → tap empty / tap one cell → only that interaction's
      selection chrome remains (group rings gone)
- [ ] `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build:web`
