# 2026-08-04

## P3.1 — Chain layout pass

### Session log

1. Built `src/chains/layout.ts`: `layoutChain(chain, nodes, locale) → Record<NodeId, Vec2>`,
   the §8.1 flush left-to-right algorithm as a pure function — no store, no React. A member
   id with no matching node (deleted but not yet pruned from `members`) is skipped rather
   than thrown on; layout is a read pass, not the place that enforces `members` consistency.
2. Wired it into `store/commands.ts` via a new `reflowChain(draft, chainId)` helper, used in
   two places:
   - `appendMembersToChain` (P2.8's typing-driven chain builder) now calls it once after
     pushing new members, replacing the inline flush-layout loop that P2.8 had duplicated
     directly into the command (see the `2026-08-04` revision below on decision #16).
   - `setNodeRaw` now calls it whenever the edited node belongs to a chain, so a raw edit
     that changes `widthOf` (§8.1) re-flows the rest of the chain in the same commit. This
     did not exist before this task — P2.8 only ever wrote a member's position once, at
     append time.
3. Verified live in a real browser (not just `tsc`/tests, per the standing `2026-08-03`
   revision 8 rule): built `1 + 2` by typing, then tapped `1` back into edit mode and
   retyped it as `999999`. The operator and `2` visibly shifted right in the same frame,
   with the operand showing its locale-grouped `999,999` mid-edit — confirming §8.1's "no
   frame ever renders a stale layout" holds for the edit path, not only the append path.
4. 9 new tests in `src/chains/layout.test.ts` (flush layout, no-gap adjacency, a
   single-member chain sitting at the anchor, a missing member id being skipped, reordering
   `members` reordering the output, and — the §6.1 guarantee — two nodes carrying identical
   stale `position.x` values still laying out strictly by `members` order) plus one new
   `commands.test.ts` case asserting a raw edit re-flows a chain's later members. Full suite:
   204 passing (was 195).

### Knowledge revisions

**3. Decision #16 ("typing's chain-building is a separate, simpler mechanism than P3's") did
not mean the position-computation formula itself had to stay duplicated.**
*Previously believed:* implicitly, by leaving `appendMembersToChain`'s inline flush-layout
loop untouched going into this task — decision #16 (`2026-08-04`, P2.8 entry) reads as
"P3's mechanism and P2.8's are different and shouldn't be conflated," which could be
mis-read as "don't touch P2.8's code from a P3 task."
*Now known:* decision #16 is about the *candidate-resolution* mechanism (§8.2-8.4's
geometric search for which chain a dragged node joins) being unnecessary for typing, which
always knows its target chain already. It says nothing about the §8.1 flush-*layout*
arithmetic once both call sites need it — that was always meant to be shared (`measure.ts`'s
own header comment: "Reused verbatim by P3's chain layout"). `appendMembersToChain` now
calls `layoutChain` instead of re-deriving the same formula inline, and `setNodeRaw` gained
a re-flow it never had, which decision #16 was never a reason to withhold.
*What established it:* re-reading `measure.ts`'s and `commands.ts`'s existing header
comments together while implementing P3.1 — both already pointed at this outcome, so no new
evidence was needed, only noticing the two files' comments hadn't been reconciled yet.

## Session log

1. Implemented P2.9 — long-press context menus (§8.6).
   - Added `contextMenu` and `groupSelectedIds` ephemeral state to `uiStore`.
   - Added `selectGroup` command to `commands.ts`: selects every node in the same chain,
     or a singleton set for a free (unchained) node. No document mutation; no undo entry.
   - Created `src/nodes/NodeContextMenu.tsx`: `NodeContextMenu` (Copy disabled, Delete,
     Select group), `CanvasContextMenu` (Add number / Paste / Add graph all disabled per
     §17.2), and `ContextMenuOverlay` driven by uiStore state.
   - Added `onLongPress` prop to `Canvas.tsx` using `Gesture.LongPress()` at 500 ms.
   - Wired long-press in `AppShell.tsx`: hit-tests the world point, dispatches node or
     canvas menu to uiStore.
   - Updated RNGH Jest mock to include `LongPress` and `minDuration` — the mock only
     stubbed the four gestures used before this task.
   - 12 new tests; full suite now 163 passing.

## Findings

### Toolchain

**react-native-gesture-handler manual mock omits newly used gestures.**
The project maintains a manual RNGH mock at `__mocks__/react-native-gesture-handler.js`
(rather than transforming the package) because the real implementation requires a native
UI thread. Any new `Gesture.*` call added to production code will break the `App.test.tsx`
render test with `TypeError: Gesture.LongPress is not a function`. Fix: add the new
gesture and any new chainable methods to `makeGestureBuilder`'s `chainable` array and the
`Gesture` object.
*Misleading symptom:* the error appears inside the `Canvas` component render, which looks
like a Canvas bug rather than a missing mock.

### Correctness

**`Gesture.Race` combined with `LongPress` is the right composition for cancellation.**
Pan activates before the long-press timer fires, so a scroll attempt cancels the menu
gesture without any extra logic. This matches the precedence decision below.

## Knowledge revisions

**1. Long-press precedence: context menu wins over P3.7 chain-drag.**
*Previously believed:* the right order was unresolved — §17.1 explicitly deferred it to
P3.
*Now known:* context menu (500 ms long-press) takes precedence over P3.7 long-press-to-
move-chain. P3.7 must check `contextMenu !== null` before activating chain-drag mode.
The motivation: a deliberate-looking hold on a node should always produce the menu first;
chain-drag is a secondary affordance. This is one line in whatever gesture the chain-drag
uses; it is deliberately not baked into Canvas (which stays a generic transform container).
*What established it:* §17.1 was a deferred open question. The 500 ms gesture being `Race`d
with `Pan` already makes pan-during-drag safe; the only remaining ambiguity was which
long-press won. Context menu was chosen because it is the more explicit intent signal.

## Closing checklist

- [x] `npx tsc --noEmit` — clean
- [x] `npm run lint` — clean
- [x] `npm test` — 163 passed, 0 failed
- [x] Acceptance criteria ticked in `docs/DEVELOPMENT_PLAN.md`
- [x] Journal entry written

---

## P2.8 — Input dispatch: keypad and hardware keyboard

### Session log

1. Built `src/keypad/keymap.ts`: an `EditorCommand` union (`KeypadKey` plus hardware-only
   `escape`/`arrow` commands), `commandFromHardwareKey(key)` mapping a `KeyboardEvent.key` to
   the same commands the on-screen keypad reports (§8.5), and `dispatchEditorCommand` - the
   one function both `Keypad.tsx`'s `onKeyPress` and `AppShell.tsx`'s hardware keydown
   listener call, per the task's "one dispatch function" requirement.
2. Added chain-building commands to `store/commands.ts`: `appendNumberNode`,
   `appendParenNode`, `appendEqualsNode`, `appendOperatorAndNumber`, all going through a new
   internal `appendMembersToChain`. A key press appends to the chain the *selected* node
   already belongs to (creating a one-member chain first if it doesn't have one), computing
   the new member's position with §8.1's flush-layout formula directly - this needed no
   candidate search because typing always knows exactly which chain and index to extend,
   unlike P3's drag-based snapping (§8.2-8.4), which has to disambiguate several nearby
   candidates a *dragged* node might mean to join. Recorded as decision #16 - this is a
   narrower, simpler mechanism than P3's, not a preview of it, and the two shouldn't be
   conflated later.
3. Pressing an operator appends the operator **and** a fresh empty number node in the same
   undo entry (`appendOperatorAndNumber`), so the next keystroke lands in a new operand and
   undo can't strand an operator without one.
4. Wired `AppShell.tsx`'s hardware-keyboard listener and `Keypad.tsx`'s `onKeyPress` prop to
   `dispatchEditorCommand`; added `lastInteractionPoint` to `uiStore` (set on every canvas
   tap) so a key press with nothing selected has somewhere to create its node, matching
   §8.5's "otherwise creates a new node at the caret/last-tap point."
5. Pressing anything but Backspace/Escape/arrows with a `result` node selected is a
   deliberate no-op with a `TODO(P4.9, §8.7)` comment in `dispatchEditorCommand` - continuation
   doesn't exist yet, and a half-built version of it would be a placeholder behaviour users
   would have to unlearn later.
6. Extended `NumberNode.tsx`'s existing `onKeyPress` (previously only Backspace/Escape) to
   route every other recognised key through the same `dispatchEditorCommand`, so a hardware
   keyboard can complete a chain while a number is being edited, not just when nothing is
   focused. Digits, the decimal separator, and a non-empty Backspace are still left to
   `onChangeText`/`parseUserInput`, which already normalise them per locale.
7. Verified end-to-end in a real browser (not just `tsc`/tests): tapping empty canvas, typing
   a full `12 + 34 =` chain on a real keyboard, the same chain via on-screen keypad taps, and
   a mixed hardware+keypad sequence all produced one correctly-ordered chain with no stray
   nodes. Backspace on a selected non-editing node deletes it (the only path that currently
   exercises "delete works," since P2.9's context menu doesn't exist yet).
8. Ticked all five P2.8 acceptance boxes and P2's progress line in `DEVELOPMENT_PLAN.md`.

### Findings

#### Toolchain

- **`react-native-gesture-handler`'s web backend treats a bubbling `Enter` or `' '` keydown
  reaching a `GestureDetector`-wrapped view as that gesture's keyboard-activation
  equivalent, regardless of what element is actually focused.** Its
  `KeyboardEventManager.registerListeners()` (`web/tools/KeyboardEventManager.js`) attaches a
  plain native `keydown` listener directly on the gesture's own view, checking only
  `event.key` against `['Enter', ' ']` - it does not check whether that view itself has
  focus. `NumberNode`'s `TextInput` is a DOM descendant of `Canvas`'s `GestureDetector`-wrapped
  view, so pressing Enter while editing a number bubbles straight through it before React's
  own synthetic dispatch even runs (React 17+ delegates all synthetic events to the DOM root,
  so a plain ancestor `addEventListener` between the target and the root always sees a
  bubbling native event first, and calling `stopPropagation()` from inside a React handler -
  including react-native-web's own `TextInput.handleKeyDown`, and `preventDefault()` for that
  matter - happens too late to stop it). The result: completing a chain with Enter also
  silently completed Canvas's `Tap` gesture, at the position of whatever `keydown`'s
  `event.target` was, creating a stray free number node.
  **Misleading symptom:** the extra node appeared only after `=`, at a screen position with
  no visible connection to anything just typed, and it reproduced identically whether or not
  `blurOnSubmit`/`preventDefault` were used on the TextInput - both looked like plausible
  fixes and neither touched the actual cause, because the cause wasn't in React's event
  handling at all.
  **Fix:** `NumberNode.tsx` now adds its own **raw** `keydown` listener directly on the
  input's host DOM node (reached via a plain `ref`, which react-native-web forwards straight
  to the `<input>`) and calls `stopPropagation()` there for `Enter`/`' '` before the event can
  reach Canvas's ancestor listener - dispatching `=` itself from that same raw listener, since
  once propagation is stopped React's own `onKeyPress` for that keystroke never fires either.
  **Generalisable:** any focusable/editable element nested inside a `GestureDetector`-wrapped
  view on web can trigger that gesture via Enter/Space, independent of real pointer input;
  worth checking again if a future feature nests another interactive control inside a gesture
  view.

- **A plain `TouchableOpacity` press starts with a `mousedown`, which blurs whatever
  `TextInput` currently has focus before `onPress` fires.** `NumberNode`'s `onBlur` is
  `deselectNode`, so clicking any on-screen keypad key while editing a number cleared
  `selectedNodeId`/`editingNodeId` *before* that key's `dispatchEditorCommand` call ran -
  every key after the first landed as its own new free node instead of continuing the one
  being edited, since dispatch saw "nothing selected" every time.
  **Misleading symptom:** the very first keypad key press after a tap worked correctly (there
  was nothing focused yet to steal focus from); only the second and later presses in the same
  sequence silently stopped chaining, which reads like a chain-building bug rather than a
  focus-order one.
  **Fix:** every data-entry key in `Keypad.tsx` (`Key`, `DigitKey`, `OperatorKey`,
  `EqualsKey`) now takes `onMouseDown={(e) => e.preventDefault()}` - the standard fix for an
  on-screen keyboard's keys not stealing focus from the field they edit. Unrecognised (and
  harmless) on native, where `TouchableOpacity` has no mouse events to steal focus with.
  **Generalisable:** any future on-screen pressable meant to act on a currently-focused text
  field needs the same guard; it's easy to verify only with real clicks in a browser; a jest
  render test that just calls `.props.onPress()` directly (as this project's existing keypad
  tests do) can't see it, since it skips the DOM mousedown entirely.

### Knowledge revisions

**1. Chain construction was assumed to be entirely P3's job.** *Previously believed:*
`store/commands.ts`'s own header comment ("Chain mutations - snap, drag, detach - land in P3
once the layout pass exists to give them something to act on") plus P2.4/P2.7's addenda,
which explicitly deferred chain assembly to later phases. *Now known:* typing needs its own,
much simpler chain-building path - appending to *the chain the selected node already belongs
to* is a one-line lookup, not the geometric candidate search P3's drag/snap has to run
against several nearby free nodes - so it doesn't actually depend on P3's snapping machinery
and can (and per this task's acceptance criteria, must) exist before P3 does. Recorded as
decision #16 so this isn't re-derived as a P3 duplicate later. *What established it:* P2.8's
own acceptance criterion ("completing a full chain by typing") is unsatisfiable without some
chain-building mechanism, and none of P3's tasks are prerequisites for the simpler,
selection-anchored version this task needed.

**Standing lesson:** "P3's job" in this file's task headers describes the *drag-gesture*
mechanism specifically (§8.2-8.4's candidate resolution), not "anything that touches
`document.chains`" - a deterministic, non-geometric way to extend a chain can land earlier
if a task's acceptance criteria require it.

### Open threads

- **Continuation (§8.7) is still unimplemented**, tracked by the `TODO(P4.9)` in
  `dispatchEditorCommand` rather than a placeholder. What would close it: P4's engine
  landing far enough to produce a `ResultNode`, plus a reference-node creation command - both
  outside this task's dependencies (P2.3, P2.6, P2.7 only).
- **`appendMembersToChain` doesn't create a `ResultNode` when `=` is appended**, even though
  §9's state diagram shows "`=` appended → result node created" as one transition. Left out
  deliberately: no engine exists yet to give a result a value, and `widthOf`'s own comment for
  the `result` case already says as much ("No engine yet (P4)"). Whichever P4 task wires
  evaluation should decide whether it creates the `ResultNode` reactively (watching for a
  chain becoming `Valid` with a trailing `=`) or whether an append command needs to create an
  empty one for it to fill in - either is compatible with what's here, since a chain's
  `members` array never includes its result node in this codebase's model (§6: `ResultNode`
  points to its chain via `sourceChainId`, not the other way around).
- **Arrow-key chain navigation (`dispatchEditorCommand`'s `arrow` case) is implemented and
  unit-tested but has no chain to navigate yet in normal use** - the only way a chain exists
  at all before P3 lands is via this task's own typing-driven `appendMembersToChain`. Once
  P3's drag/snap-built chains exist too, worth a real-device pass to confirm arrow navigation
  still reads correctly across a chain assembled by dragging rather than typing.

---

## P2 phase review and close-out

### Session log

1. Asked to review every P2 task for correctness before starting P3. All ten were already
   ticked in `DEVELOPMENT_PLAN.md` (P2.1–P2.10, landed across several sessions/PRs since the
   last time this file was touched). Ran the full verification suite on `main`
   (`tsc`/`lint`/`test`/`build:web`, 195 tests) - clean - then read every touched file for
   P2.4 through P2.10 against its own acceptance criteria (P2.1–P2.3 and P2.7 had already been
   reviewed in an earlier session). All matched, including the less obvious ones: `setNodeRaw`
   throws rather than silently no-opping on a non-number node (P2.4), per-node Zustand
   selectors genuinely scope re-renders (P2.4/P2.5), the tap/pan-vs-long-press gesture
   precedence is a documented deliberate choice with a recorded rationale (P2.9), swipe-clear
   only mutates the document on explicit confirm (P2.10).
2. Then did what the phase exit check actually asks for - "demonstrated by hand in a browser,
   in one sitting" - for real, with Playwright driving the actual webpack dev server rather
   than trusting the boxes already ticked. This is the check `2026-08-03` revision 8 exists to
   keep from being skipped again. It caught a real bug (below) that eighteen passing test
   suites and a prior "verified in a real browser" journal claim (P2.8's own entry, session log
   item 7) had both missed.
3. Fixed the bug, confirmed the fix with the same live-browser repro, reran the full suite
   (still 195 green), then re-verified the whole phase-exit checklist live: tap-to-edit,
   every keypad region, a full `1221 + 3 - 20 =` chain typed on a real (virtual) keyboard,
   long-press → Delete, `"3."` surviving a decimal keypress, and `13,5`-style grouped display
   under an emulated `de-DE` locale.
4. Found `DEVELOPMENT_PLAN.md`'s P2 status line stale for a second time (see `2026-08-03`'s
   own progress-count fix) - it read "9/10" when all ten tasks were done. Fixed it and the
   phase-exit box together, struck through the P2 heading in the status table to match P0/P1.

### Findings

#### Correctness

- **`react-native-web`'s `TextInput.onKeyPress` fires from a native `keydown` listener without
  calling `preventDefault()` for you** (`TextInput/index.js`'s `handleKeyDown`: it calls
  `onKeyPress(e)` and only conditionally prevents default for `Enter`). If an app's own
  `onKeyPress` handler responds to a key by synchronously changing which node is being edited
  - as `NumberNode.tsx`'s did for every operator/paren/backspace-on-empty key, via
  `dispatchEditorCommand` - React re-renders and moves focus to a *different* `<input>` before
  the browser gets to apply that keydown's own default action (inserting the character). The
  browser then applies it to whatever is focused *now*, not to the element the keydown actually
  started on. Concretely: typing `3` then `-` fired `appendOperatorAndNumber`, which creates a
  fresh empty operand and switches `editingNodeId` to it, synchronously, inside the `-`
  keydown's own handler - and the still-in-flight default action then inserted `-` into that
  *new*, just-focused input, which should have started empty. Typing `4` next gave `"-4"`, not
  `"4"`: a hardware-typed subtraction silently negated its second operand.
  **Misleading symptom:** `+` triggers the exact same race and the exact same leaked keystroke,
  but produces no visible symptom, because `+` isn't valid canonical raw (`isCanonicalRaw`
  requires an optional **leading** `-` only) - `parseUserInput` silently rejects it and the
  keystroke vanishes without a trace. P2.8's own journal entry (session log item 7) verified a
  hardware-typed `12 + 34 =` chain and reported it correct, which was true, and also happened
  to be the one operator that couldn't have shown this bug.
  **Fix:** `handleKeyPress` now calls `e.preventDefault()` for every key it's about to hand to
  `dispatchEditorCommand`, so the browser never performs the default insertion regardless of
  which input ends up focused afterward. Digits and the decimal separator are unaffected -
  they still return early and go through `onChangeText`/`parseUserInput` as before.
  **Generalisable:** any handler that changes DOM focus synchronously in response to a keydown,
  without cancelling that keydown's default action, risks the browser applying the default to
  the new focus target instead of the original one. Test operator/command keys that also
  happen to be valid input characters (`-`, `.`) specifically - they're the ones that turn this
  class of race into a silent data-corruption bug instead of a silently-dropped keystroke.

### Knowledge revisions

**2. "Verified with a real keyboard in a browser" (P2.8) was true and still missed a real bug.**
*Previously believed:* P2.8's journal entry recorded a real-browser hardware-keyboard
verification (typing `12 + 34 =`) as satisfying its own acceptance criterion, and the box was
ticked on that basis.
*Now known:* the verification was genuine - `+` really was typed on a real keyboard and really
did work - but the specific example chosen happened to be immune to a bug that a different,
equally reasonable example (`3 - 4`) would have shown immediately, because `+` isn't valid
canonical raw and `-` is. "Verified by hand" is only as strong as the cases the hand actually
tried; a single example that happens to avoid the one class of input the bug depends on reads
identically to a clean pass.
*What established it:* redoing the same class of check (real Chromium, real keyboard) with a
different operator, as part of reviewing all of P2 before starting P3, per this session's own
task.
*Standing lesson:* a manual verification step's acceptance criterion should name the *specific*
inputs that exercise the risky path, not just "type a chain" - especially where a sign
character and an operator share a keystroke (`-`/`−`), which is exactly the kind of coincidence
that makes one example non-representative of a whole class of input.

### Closing checklist

- [x] `npx tsc --noEmit` - clean
- [x] `npm run lint` - clean
- [x] `npm test` - 195 passed, 0 failed (unchanged count - the fix touched dispatch behaviour a
      unit test wasn't exercising; see the standing lesson above for why the bug needed a real
      browser rather than another unit test to surface at all)
- [x] `npm run build:web` - emits `dist/`
- [x] P2 phase exit check demonstrated live and ticked in `DEVELOPMENT_PLAN.md`, with the bug
      found and fixed in the same pass
- [x] Journal entry written

---

## P3.2 — Node bounds and neighbour queries

### Session log

1. Added `src/chains/bounds.ts` as the geometry layer P3's snapping search needs: `boundsOf`, `verticalOverlap`, `memberBoundaries`, and `makeSnappingNeighbours`. The implementation is deliberately O(n) per §8.4, but behind a tiny `SnappingNeighbours` interface (`chainsNear`, `freeNodesNear`) so P3.3 can call a stable surface when the scan is later replaced with a spatial hash.
2. Kept the helpers pure and layout-driven rather than store-aware: node bounds come from `position` + `widthOf` + `tokens.nodeHeight`, chain bounds from `chain.anchor` plus the summed member widths, and insertion boundaries from the same left-to-right accumulation §8.1 uses. Like `layoutChain`, missing member ids are skipped rather than thrown on, so a transiently stale `members` array does not crash a read-only geometry pass.
3. Added focused tests in `src/chains/bounds.test.ts` for exact threshold behaviour, not just obvious inside/outside cases: boxes that only touch vertically still count as zero-gap, a candidate whose vertical gap is exactly `SNAP_VERTICAL - 1` is included, and one at exactly `SNAP_VERTICAL` is excluded by §8.3's strict `< SNAP_VERTICAL` comparison. Also covered self-exclusion, chained-vs-free filtering, and insertion boundaries between adjacent chain members.
4. While writing those tests, found and fixed my own first implementation error: I had compared against `tokens.snapVertical`, but snapping thresholds are interaction constants from §8.2, not design tokens from §1.2. Because no such token exists, every neighbour filter silently returned empty. The fix was to export named `SNAP_DISTANCE`, `SNAP_VERTICAL`, and `DETACH_DISTANCE` constants from `bounds.ts`, which P3.3 can now import instead of re-inlining numbers.

### Knowledge revisions

**4. Snap-threshold tests must be written against bounding-box gaps, not anchor deltas.**
*Previously believed:* placing a candidate chain at `dragged.y + SNAP_VERTICAL` exercised the exact vertical-threshold boundary.
*Now known:* `verticalOverlap` compares the gap between bounding boxes, so with 64dp-tall nodes a candidate whose `top` is only 48dp below the dragged node still overlaps it by 16dp and therefore has a vertical gap of `0`, not `48`. The real threshold case is `candidate.top = dragged.bottom + SNAP_VERTICAL` (outside) and one unit less (inside).
*What established it:* the first version of `bounds.test.ts` failed while the helper logic was otherwise correct; recomputing the geometry from `tokens.nodeHeight` and the actual `verticalOverlap` formula showed the test setup, not the algorithm, was wrong.

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test`
- [x] `npm run build:web`
- [x] Acceptance criteria ticked in `docs/DEVELOPMENT_PLAN.md`
- [x] Journal entry written

---

## P3.3 — Snap candidate resolution

### Session log

1. Implemented `src/chains/snapping.ts`: pure `resolveSnapCandidate(dragged, neighbours, nodes, locale)` returning `prepend` / `append` / `insert` / `newChain` / `null`, gathering §8.3 candidates through P3.2's `SnappingNeighbours` and keeping the nearest by the same horizontal distance that passed the threshold. Thresholds imported from `bounds.ts` (`SNAP_DISTANCE` etc.) — never inlined.
2. Table-driven tests in `src/chains/snapping.test.ts` at each threshold boundary (strict `< SNAP_DISTANCE`), both NEW_CHAIN member orders, nearest-wins between a chain and a free node, vertical gating inherited from neighbours, own-chain exclusion while still a member, and the §8.2 hysteresis case (`DETACH_DISTANCE > SNAP_DISTANCE` → just-detached node does not re-snap into the vacated slot).
3. While writing the INSERT cases, found that a center placed "just inside" one boundary can also fall inside `SNAP_DISTANCE` of the neighbouring boundary when members are narrow (operator width 34 is only 6 above `SNAP_DISTANCE` 28). Nearest-wins then returns the other index. Tests place the center on the side of boundary 1 away from boundary 2, and align exactly with boundary 2 for the second-index case.

### Findings

**INSERT threshold tests must keep neighbouring boundaries out of range.**
`memberBoundaries` are only one member-width apart. With `operatorWidth = 34` and `SNAP_DISTANCE = 28`, a dragged center within 28 of boundary *i* is often also within 28 of boundary *i±1*, and nearest-wins picks the closer one. An "INSERT at index 1 just inside threshold" fixture that sits between two boundaries will silently assert the wrong outcome (or pass for the wrong reason). Place the center on the outer side of the target boundary, or align it exactly when adjacent spacing is `≥ SNAP_DISTANCE`.

### Knowledge revisions

*(none — §8.3's pseudocode mapped onto the P3.2 neighbour interface without overturning a prior belief.)*

### Open threads

- Equal-distance ties between candidates are unspecified in §8.3. This implementation keeps the first candidate in gather order (chain prepend → append → inserts L→R, then free-node new-chains). Revisit if a real drag produces a visible caret flicker at a midpoint.
- Detach distance measurement itself (from home slot vs. from chain AABB vs. from press origin) is P3.5's concern; P3.3 only proves that once a free node sits `DETACH_DISTANCE` from a slot, resolve returns none.

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test`
- [x] `npm run build:web`
- [x] Acceptance criteria ticked in `docs/DEVELOPMENT_PLAN.md`
- [x] Journal entry written

---

## P3.4 — Chain mutation commands

### Session log

1. Added §8.3 bookkeeping helpers in `store/commands.ts`: `removeResultNodesForChain`, `finalizeChain` (drop result if `=` gone → delete empty → dissolve single-member → else reflow), and `removeFromCurrentChain`. Typing's `appendMembersToChain` and `deleteNode` now go through `finalizeChain` too, so dissolve/result rules are not snap-path-only.
2. Exported `prependToChain`, `appendToChain`, `insertIntoChain`, `formNewChain`, `detachNode`, plus `commitSnapOutcome` as a thin dispatcher over P3.3's `SnapOutcome` for P3.5. Each is one `applyCommand` / one undo entry. Layout runs in the same commit via `finalizeChain`.
3. Updated the P2.3 `deleteNode` test that expected a leftover one-member chain to survive: §8.3 dissolves it. Added focused P3.4 tests for append/prepend/insert/new-chain/detach, dissolve-on-detach, equals-loss deleting the result, undo/redo, and no-ops.

### Findings

**`deleteNode` previously left one-member chains intact; §8.3 forbids that.**
P2.3 only required "no dangling id in `members`" and deferred re-layout to P3. Its test asserted that deleting one of two members left a one-member chain. P3.4's dissolve rule makes that state illegal — a sole survivor becomes free with an authoritative `position` and the chain is deleted. `deleteNode` now shares `finalizeChain` with the snap commits so the two paths cannot drift.

### Knowledge revisions

*(none — §8.3's bookkeeping was implemented as specified; the deleteNode test update was aligning an earlier, weaker consistency rule with the architecture, not overturning a design belief.)*

### Open threads

- Result nodes are still never *created* here (P4.7). The equals-loss path is tested by seeding a synthetic result; live create→snap→`=`→result waits on the engine.
- Mid-drag detach vs release-time detach is P3.5; these commands only commit the outcome.

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test`
- [x] `npm run build:web`
- [x] Acceptance criteria ticked in `docs/DEVELOPMENT_PLAN.md`
- [x] Journal entry written

---

## P3.4 follow-up — prepend shifts `chain.anchor`

Addresses the review on PR #59 (`#issuecomment-5174576098`).

### Session log

1. Fixed `prependToChain`: before unshifting, move `chain.anchor.x` left by
   `widthOf(node, getDeviceLocale())` so existing members keep their world positions and
   only the new leftmost cell is new. Same geometry applied in `insertIntoChain` when the
   clamped index is `0` (snap never yields that via `memberBoundaries`, but a direct call
   can).
2. Corrected the prepend test that had locked in the wrong behaviour ("reflows from the
   existing anchor"). Added assertions that pre-existing member positions are unchanged;
   covered `commitSnapOutcome` for `prepend` / `insert` / `newChain`; added undo→redo
   round-trips for prepend/append/insert/detach; aligned `detachNode`'s position write with
   the file's `{ ...position }` style. `seedChain` in the P3.4 suite now runs `layoutChain`
   so the "members stay put" baseline is a laid-out cache, not the `{0,0}` from
   `addNumberNode`.

### Findings

**Prepend must move `Chain.anchor`, not only `members`.**
`layoutChain` always starts at `anchor.x` and walks `members` in order. Unshifting without
shifting the anchor places the *new* node at the old left edge and slides every prior
member right by `widthOf(newNode)` — the opposite of §8.3's "opens a gap at the pending
insertion point." Append and interior insert are fine with a fixed anchor; left-edge
insertion is the special case.
*Misleading symptom:* a green prepend unit test asserting `anchor` stays at `{x:100,y:40}`
looks like correct reflow coverage, but it encodes the jump.

### Knowledge revisions

**5. Prepend commit leaves the chain's prior members unmoved.**
*Previously believed (and asserted by the P3.4 prepend test):* prepending reflows from the
existing `anchor`, so the new node lands at the old left edge and prior members shift right.
*Now known:* a prepend opens the gap on the *left* — `anchor.x -= widthOf(newNode)` — so
prior members keep their exact positions. Index-0 `insertIntoChain` is the same geometry.
*What established it:* PR #59 review against §8.2/§8.3 feedback text plus
`layoutChain`/`memberBoundaries` (interior indices only; left-edge snaps are exclusively
the `prepend` outcome kind).
*Standing lesson:* when layout truth is `anchor` + `members`, a membership change at the
left edge is an anchor mutation, not only a list mutation — and a test that freezes the
anchor will hide it.

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test`
- [x] `npm run build:web`
- [x] Acceptance criteria ticked in `docs/DEVELOPMENT_PLAN.md` *(unchanged — still P3.4)*
- [x] Journal entry written

## P3.5 — Node drag gesture

### Session log

1. Added `useNodeDrag` + `dragLifecycle` (pure release decisions), wired through `NodeLayer` with a per-node `GestureDetector`. Mid-drag position is a Reanimated transform; document commits (`commitSnapOutcome` / `detachNode` / `moveFreeNode`) run only on release. Live candidates publish to `uiStore.dragSnap` for P3.6.
2. `CanvasViewportContext` exposes pan/zoom shared values; a DEV-only `window.__CALCMIND_VIEWPORT__` probe was added so zoom 0.25 / 4 checks can set zoom without a real pinch.
3. Fixed two bugs found under Playwright (RNGH does respond to CDP mouse events; the computer-use agent's synthetic events do not):
   - Animated style left the previous `translate` stuck after release when the idle branch omitted `transform` — always set `translateX/Y: 0` when not dragging.
   - Syncing shared values during render tripped Reanimated strict mode; moved that sync into `useEffect` and re-seed on `beginDrag`.
4. Verified with Playwright: snap at zoom 1 / 0.25 / 4, detach, empty-canvas pan, transforms cleared after release.

### Findings

**RNGH web ignores some synthetic mouse events but accepts Playwright/CDP `Input.dispatchMouseEvent`.**
The computer-use agent's click-drag left nodes unmoved; Playwright's `page.mouse` path (Chrome DevTools Protocol) activates `Gesture.Pan` correctly. Same class of "jest can't see the gesture" limitation already recorded for keypad swipe — prefer CDP/Playwright for interactive web gesture checks.

**Reanimated animated styles must clear `transform` explicitly on idle.**
Returning `{ zIndex: 0 }` after a drag left `matrix(..., tx, ty)` on the view while the store position had already updated, so the node appeared offset from its authoritative `left`/`top`. Always emit `transform: [{ translateX: 0 }, { translateY: 0 }]` when not dragging.

**Pan `translationX/Y` are screen pixels even for detectors inside Canvas's scale.**
At zoom 0.25, a 20px screen drag moved ~14 world units without `/zoom` and the expected ~screen/zoom amount with it. Node drag must divide by the live zoom shared value (same as Canvas pan) so §8.2 thresholds stay world-unit invariant.

### Knowledge revisions

*(none beyond the findings above — §8.2's state machine mapped cleanly onto release-time P3.4 commits.)*

### Open threads

- MovingChain / long-press-to-move-chain is P3.7; context menu still wins at 500ms per the P2.9 decision.
- Insertion caret / gap feedback is P3.6 (`uiStore.dragSnap` is the feed).
- Left-edge snap commit geometry (shift `anchor` on prepend) landed in the P3.4 follow-up on `main` before this rebase; drag uses those commands as-is.

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test`
- [x] `npm run build:web`
- [x] Interactive check at zoom 0.25 and 4 (Playwright)
- [x] Acceptance criteria ticked in `docs/DEVELOPMENT_PLAN.md`
- [x] Journal entry written

---

## P3.5 review follow-up (PR #60)

### Session log

1. Added tests locking in `formNewChain(leftPosition)` and `commitSnapOutcome(..., position)` for both NEW_CHAIN directions: dragged-as-left seeds the anchor from the live release point; dragged-as-right leaves the stationary left node's store position alone.
2. Stripped the DEV-only `window.__CALCMIND_VIEWPORT__` probe from `Canvas.tsx` — the zoom 0.25/4 verification is already recorded above; no reason to keep permanent surface area in a core component for a one-shot harness.
3. Documented why `endDrag` reads `uiStore.dragSnap` (Reanimated `runOnJS` FIFO) rather than a session-ref candidate.

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test`
- [x] Journal entry written

---

## P3.6 — Insertion feedback

### Session log

1. Added `insertionFeedback` to `src/chains/layout.ts`: pure §8.3 mid-drag preview that
   returns a caret rect and per-node `translateX` offsets sized to `widthOf(dragged)`.
   Geometry matches the P3.4 commit rules (revision 5 earlier today) — prepend/index-0
   opens on the left without shifting members; interior insert shifts members at `i` and
   after; append leaves members put; `newChain` shifts the partner only when the dragged
   node will become leftmost. The dragged id is stripped from target-chain `members` for
   the preview so a mid-drag detach that re-approaches its home chain does not count the
   ghost slot twice.
2. `NodeLayer` reads P3.5's `uiStore.dragSnap` feed, applies offsets as temporary `left`
   deltas (not document writes), and draws an `insertion-caret` view at the slot. Offsets
   and caret both clear when `candidate` is null, so leaving range closes the gap with no
   residual. `left` rather than a competing `transform` so Reanimated's drag translate on
   the grabbed node is undisturbed.
3. 8 new tests in `layout.test.ts` covering null-candidate clear, prepend/append/insert/
   index-0, both `newChain` directions, and a missing chain id. Full suite 270 passing.

### Findings

**Gap preview must use `left` offsets, not a second `transform`.**
`PositionedNode` already applies a Reanimated `transform` for the drag delta. In RN, later
`transform` style entries replace earlier ones rather than composing, so a gap
`translateX` written beside the animated style is wiped to `0` on every frame while idle
siblings are "not dragging." Shifting via `left: position.x + gapOffset` sidesteps the
conflict and also matches commit geometry: on release, `clearDragSnap` and
`commitSnapOutcome` land in the same synchronous turn, so React paints once with
`gapOffset = 0` and the laid-out `position.x` already advanced — no jump.
*Misleading symptom:* members twitch back to flush for one frame on snap commit even though
the final layout is correct.

**"No store write per frame" means the document store.**
P3.5 already publishes `uiStore.dragSnap` every drag frame for this task to consume. P3.6
adds no document/`applyCommand` writes mid-drag; the gap is a view-only offset. A true
UI-thread-only caret (shared values, no JS store) would mean redoing P3.5's candidate
publish path — out of scope here and not required once the ephemeral/document split is
respected.

**Caret colour must contrast with the canvas, not with labels.**
First pass used `labelColor` (`#3B4252`). Against the dark canvas it is effectively invisible —
Playwright found the DOM node at the right place while the screenshot showed nothing. Switched
to `#F3F4F6` and floored caret width at 4dp. Same class of failure as the 0×0 web page: the
structure was correct and the eye was the only detector.
*Misleading symptom:* `insertion-caret` testID count is 1 and `boundingBox` is non-null, yet the
screen looks unchanged.

### Knowledge revisions

*(none — feedback geometry followed revision 5's prepend rule rather than overturning it.)*

### Open threads

- ~~P3.7 MovingChain / long-press-to-move-chain still open~~ — closed below (PR #61).
- Phase exit check for P3 still needs the hand demo at more than one zoom.
- True worklet-driven gap offsets (no `uiStore` round-trip) remain available if drag-frame
  JS cost shows up in a profile; not pursued without a measurement.

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test`
- [x] `npm run build:web`
- [x] Interactive check: caret + gap visible on insert/prepend/append; clears out of range
- [x] Acceptance criteria ticked in `docs/DEVELOPMENT_PLAN.md`
- [x] Journal entry written

## P3.6 review follow-up (PR #63)

### Session log

1. Fixed `insertionFeedback`'s `newChain` / dragged-as-left branch: it previously shifted the
   partner by a flat `widthOf(dragged)` from the partner's store home and parked the caret
   there. `formNewChain` anchors at the *live* release point, so the preview disagreed by
   up to ~`SNAP_DISTANCE` (and more once y differs) and jumped on commit — the exact
   artifact P3.6 exists to prevent. Threaded `livePosition` through from `dragSnap.position`,
   preview the partner at `live + gap`, and carry a 2D offset so y tracks the release row.
2. Added a layout unit test that fails under the old home-relative math, plus a
   `commands.test.ts` round-trip that asserts the previewed partner position equals the
   post-`commitSnapOutcome` position for the same live point.

### Knowledge revisions

**6. `newChain` preview when the dragged node is left must use the live drag position.**
*Previously believed (and encoded by the P3.6 `newChain` dragged-left test):* the partner
shifts by `gap` from its own store home, caret at that home.
*Now known:* `formNewChain` / `commitSnapOutcome` seed the anchor from the release
`position` whenever the dragged node is `leftId`, so the partner's committed slot is
`live + widthOf(dragged)`. Preview must use the same reference or the join jumps on release.
*What established it:* PR #63 review against `commands.ts` `formNewChain(leftPosition)` and
the failing arithmetic on the PR's own test fixtures (partner@200 vs live@120 → 80wu miss).

---

## P3.7 — Chain move vs member detach

### Session log

1. Implemented `moveChain(chainId, anchor)` — one undo entry, reflows members in the same
   commit via existing `reflowChain` (§8.1).
2. Extended `dragLifecycle` with `resolveNodeDragMode` and `LONG_PRESS_MOVES_CHAIN` (the
   one-line §17.1 flip). Precedence: free → free; `Select group` → moveChain; open context
   menu → never moveChain; else the dwell mapping at `CHAIN_MOVE_HOLD_MS = 200`.
3. Wired MovingChain through `useNodeDrag`: press dwell measured from `onBegin` → `onStart`;
   chain mode updates `chain.anchor` on release and skips snap/detach. Sibling members follow
   via module-level Reanimated shared values (`chainDragShared.ts`) so the formula translates
   as a unit without mid-drag store writes.
4. Tried both mappings interactively with Playwright/CDP against `npm run web` (RNGH ignores
   computer-use synthetic events — same finding as P3.5). Both work mechanically.
5. Shipped the §8.3 assumption: plain drag detaches, long-press-then-drag moves the chain.
   Closed §17.1; recorded decision #17. PR #61.

### Findings

**Module-level `makeMutable` needs a Jest mock export.**
`chainDragShared.ts` calls `makeMutable` at import time. The project's manual reanimated mock
only exported `useSharedValue` / `useAnimatedStyle` / `runOnJS`, so `App.test.tsx` failed with
`TypeError: makeMutable is not a function` before any assertion ran. Fix: export `makeMutable`
returning `{ value: initial }` alongside `useSharedValue`.
*Misleading symptom:* the failure is in App.test's import graph, not in dragLifecycle tests
(which never import the shared-values module).

### Knowledge revisions

**1. §17.1 mapping locked: plain drag detaches; long-press moves the chain.**
*Previously believed:* the mapping was an open assumption — opposite is defensible, decide on
a real device (§8.3 **[assumption]**, §17.1).
*Now known:* both mappings work; ship long-press → move chain, plain drag → detach. Detach /
rearrange is the frequent edit and must not require a dwell; lifting a whole expression is
deliberate and fits long-press. `Select group` stays the dwell-free alternative. Context menu
at 500 ms still wins (P2.9). Flip point remains `LONG_PRESS_MOVES_CHAIN`.
*What established it:* Playwright A/B against the live web build — plain detach, long-press
chain move, 500 ms menu, and Select-group move all verified for the shipped mapping; the
opposite mapping's plain-drag-moves-chain / long-press-detaches path also verified
mechanically, then rejected on frequency-of-use grounds. Decision #17.

### Open threads

- Phase exit check for P3 still needs a multi-zoom hand demo (P3.6 + P3.7 both landed).

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test`
- [x] `npm run build:web`
- [x] Interactive A/B of both mappings (Playwright)
- [x] Acceptance criteria ticked in `docs/DEVELOPMENT_PLAN.md`
- [x] §8.3 / §17.1 / decision #17 updated in `ARCHITECTURE.md`
- [x] Journal entry written

---

## P3.7 review follow-up (PR #61)

### Session log

1. Addressed the PR review polish items: dropped unused `Gesture.Exclusive` /
   `activateAfterLongPress` mock stubs; moved chain-sibling translate writes onto the
   UI-thread `onUpdate` worklet (was `runOnJS(updateDrag)`); documented the
   single-concurrent-chain-drag singleton assumption on `chainDragShared`; clarified that
   `heldMs` is touch-down→pan-activation, not pure still-dwell.

### Findings

**Sibling chain-follow must update shared offsets on the UI thread.**
Primary-node translate was written in the `onUpdate` worklet; sibling `chainDragDx/Dy`
went through `runOnJS(updateDrag)`, so followers lagged by at least one frame (worse under
JS load). Same class of §11.4 hot-path mistake as mid-drag store writes — the round trip
is the bug even when the payload is a shared value.
*Misleading symptom:* release-time positions looked correct in Playwright (commit path is
fine); the lag only shows mid-gesture.

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test`
- [x] Journal entry written

---

## P3 phase review and close-out

### Session log

1. Asked to start P3 and close finished issues. P3.6 and P3.7 (above) had already landed since
   the last check, closing the phase's own open thread ("phase exit check still needs a
   multi-zoom hand demo"). Reviewed both against their acceptance criteria first: `layout.ts`'s
   `insertionFeedback` is genuinely pure, cross-checked against `formNewChain`'s actual commit
   geometry in its own test (`layout.test.ts`, the PR #63 review regression), and `useNodeDrag`'s
   `resolveNodeDragMode` precedence (free → `Select group` → context-menu-open → dwell mapping)
   matches the now-closed §17.1 decision recorded in `ARCHITECTURE.md` decision #17.
2. Ran the full suite on `main` — 279 passing, `tsc`/`lint`/`build:web` clean.
3. Did the multi-zoom live demo the phase exit check actually asks for, with Playwright against
   the real dev server at zoom 0.25 and 4 (ctrl+wheel, since P3.5's review already had the
   DEV-only zoom probe stripped — see this file's P3.5 review-follow-up entry above). Snap,
   caret, detach, and no-immediate-re-snap all demonstrated at both extremes.
4. Ticked the phase exit check and marked P3 done in `DEVELOPMENT_PLAN.md`'s status table and
   diagram.

### Findings

#### Toolchain / methodology

- **A caret-visibility check can fail for reasons that have nothing to do with the caret.** My
  first zoom-0.25 pass reported the insertion caret missing mid-drag. It wasn't: a follow-up
  poll (checking caret presence at ten incremental points along the same drag) showed it
  appearing reliably while approaching the target and correctly *disappearing* again once the
  drag moved deep enough into overlap to exceed `SNAP_DISTANCE` from the other side — exactly
  the boundary-based `consider()` check in `snapping.ts` working as designed, not a miss. My
  target point for the "definitely within range" case was computed as a fraction of the node's
  on-screen width, which shrinks to a few pixels at 0.25 zoom; a target 20% short of the full
  overlap corresponds to a much larger world-unit overlap at low zoom than the same fraction
  would at zoom 1, and could land past the far side of the snap zone entirely.
  *Misleading symptom:* the check fails identically to how a real bug would present (no caret
  element in the DOM), and the failure is zoom-dependent, which reads exactly like a zoom-math
  bug. It wasn't one — the underlying candidate resolution is zoom-invariant, as P3.5 verified.
  *Standing lesson:* when a live check targets a snap/threshold boundary, compute the target
  from the same world-unit constant the code checks against, not from a screen-space fraction
  of an element that itself scales with zoom.

- **`Canvas.tsx`'s ctrl+wheel zoom handler cannot actually prevent the browser's native
  zoom/scroll.** Every ctrl+wheel notch during this session's verification logged "Unable to
  preventDefault inside passive event listener invocation" to the console. `onWheel`'s own
  `event.preventDefault?.()` call (top of the handler, `Canvas.tsx`) is a no-op here because
  the listener React (or react-native-web) attaches for `onWheel` is passive by default in this
  browser. This predates P3 — the handler is unchanged since P1 — and isn't a P3 acceptance
  criterion, so left as a discovered-but-out-of-scope finding rather than fixed here. On a real
  browser this means ctrl+wheel over the canvas may zoom the actual page alongside (or instead
  of) the app's own zoom, which the app-level zoom math otherwise handles correctly (confirmed:
  node screen width scaled by exactly 0.25 across 20 notches, so the *app's* zoom state itself
  is unaffected — this is a "browser might also zoom" UX gap, not a canvas math bug). Worth its
  own task if ctrl+wheel is meant to be a supported input, not just the two-finger-trackpad path
  it likely works fine for natively (trackpad pinch typically doesn't hit this passive-listener
  wheel path the same way a synthesized/mouse-wheel ctrl+scroll does).

### Open threads

- **Ctrl+wheel zoom's `preventDefault` is ineffective (passive listener).** Not filed as a
  numbered issue yet — flag if ctrl+wheel-to-zoom on desktop web is a path anyone actually
  relies on; the fix is almost certainly marking the `onWheel` listener non-passive (RN Web
  exposes this via a raw DOM listener option, not the `onWheel` prop itself, so it likely needs
  the same `ref`-based raw-listener pattern `NumberNode.tsx` already uses for Enter/Space).

### Closing checklist

- [x] `npx tsc --noEmit` — clean
- [x] `npm run lint` — clean
- [x] `npm test` — 279 passed, 0 failed
- [x] `npm run build:web` — emits `dist/`
- [x] P3 phase exit check demonstrated live at two zoom levels, ticked in `DEVELOPMENT_PLAN.md`
- [x] Journal entry written

---

## P4.1–P4.5 — engine core (tokenize → format)

### Session log

1. Implemented the pure half of the calculation engine (§10.1 pipeline through display
   formatting), as tasks P4.1–P4.5. No React, no store — `src/engine/` only.
   - `tokenize.ts` — `chain.members` in stored order → token stream; drops `=` and result;
     keeps numbers / operators / parens / references; partial `"3."` is a token.
   - `errors.ts` + `validate.ts` — §9 status classification. Structural validation produces
     Empty / Incomplete / Valid / Invalid / Evaluated; `Stale` and `ErrorState` live on the
     same union for P4.7–P4.8 to apply once a result exists.
   - `parse.ts` — precedence climbing with `parsePrefix` / `parsePostfix` / `parsePrimary`
     seams for the §10.2 extension path. Implicit `×` only before `(`.
   - `evaluate.ts` — decimal.js at precision 34; errors are values; nothing throws out.
   - `format.ts` — added `formatComputedValue` / `parseComputedDisplay` for the result-cell
     half of §10.3 (12 sig digs, scientific at `1e12` / `1e-6`, locale on the mantissa).
2. Table-driven tests per §14; property test that `formatComputedValue` never emits a string
   `parseComputedDisplay` rejects. Full suite still green alongside the existing engine
   (numeric / formatForDisplay) tests.
3. Tick P4.1–P4.5 in `DEVELOPMENT_PLAN.md`; P4.6 and P4.7 are now amber (ready).

### Findings

**Scientific result display cannot round-trip through `parseUserInput`.**
`formatComputedValue` emits `1.23e+12` (locale on the mantissa only). `parseUserInput` /
`isCanonicalRaw` deliberately reject `e` — `NumberNode.raw` must stay a plain decimal so a
document written in one locale opens in another without `1.020` ambiguity (decision #11).
The property test therefore undoes through a sibling `parseComputedDisplay`, not through
`parseUserInput`. Reusing `parseUserInput` for scientific would either corrupt the raw
contract or force scientific into storage.
*Misleading symptom:* "the formatter never emits something it cannot re-parse" (§14) reads
like it names the existing parse function; it names the *round-trip property*, and the
computed-value half of `format.ts` needs its own undo.

**`validateChain` does not produce `Stale` or `ErrorState`.**
Those are lifecycle statuses (§9: Evaluated → Stale on input change; Evaluated → ErrorState
on divide-by-zero / overflow / cycle). Structural validation of the member sequence cannot
observe them. The `ChainStatus` union still carries both so P4.7–P4.8 write the same type;
P4.2's "returns exactly one of …" acceptance is satisfied by the type, with the structural
subset covered by the table-driven tests. The §9 diagram transitions into Stale / ErrorState
are integration tests for the result lifecycle, not for `validate.ts`.

**Division by zero is diagnosed before `0 ÷ 0` can become `NaN`.**
decimal.js returns `Infinity` for `1/0` and `NaN` for `0/0`. We check `right.isZero()`
before dividing and always return `DivideByZero`. That prefers the more specific diagnosis
over `NotANumber` for the indeterminate form, and keeps "never Infinity" (§10.3) as a single
branch.

### Knowledge revisions

None this session — §9 / §10.2 / §10.3 / decision #3 / #4 held as written.

### Open threads

- **P4.6–P4.9** still open: error rendering on `ResultNode`, result lifecycle, recompute on
  edit, continuation. Pure engine is ready to wire.
- Carry-forward from earlier: ctrl+wheel `preventDefault` passive-listener gap; P7.7 device
  fps; P6.8 palette a11y.

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test` — 338 passed
- [x] `npm run build:web` — emits `dist/`
- [x] Acceptance criteria ticked for P4.1–P4.5 in `docs/DEVELOPMENT_PLAN.md`
- [x] Journal entry written

---

## P4.1–P4.5 review follow-up (PR #65)

### Session log

1. Addressed the PR review: fixed `formatComputedValue`'s scientific-notation decision to
   use the *rounded* magnitude (`toSignificantDigits(12)` first, then threshold), and
   documented why `tokenize` throws on a missing member while `layoutChain` skips.
2. Added regression tests for the two boundary-crossing cases the review reproduced
   (`999999999999.9` → `1e+12`; `0.0000009999999999996` → `0.000001`).

### Findings

**Scientific thresholds must be applied after significant-digit rounding.**
Deciding `|x| ≥ 1e12` / `|x| < 1e-6` on the pre-round value, then formatting the rounded
digits, lets a value that *displays* as exactly `1e12` (or `1e-6`) take the wrong branch:
`999999999999.9` was `< 1e12` pre-round so went plain, then rounded up to a 13-digit
`1,000,000,000,000`. The mirror low-end case emitted scientific for a rounded `1e-6` that
the exact-boundary test asserts is plain. Same displayed number, two forms, depending on
which side of exact the input started on.
*Misleading symptom:* the existing exact-boundary tests (`1e12`, `1e-6`, `999999999999`)
all pass, because those inputs do not move under rounding — only values that *cross* the
threshold when rounded expose it. The round-trip property test also passes: both forms
re-parse.
*Fix:* round first, decide from `significant.abs()`.

**Engine throws on dangling members; layout skips — deliberate.**
`layoutChain` skips missing ids because it is a render-side read pass over possibly
transient state. `tokenize` throws because it sits on the correctness boundary: silently
dropping a member would evaluate a truncated formula. `store/commands.ts` keeps `members`
consistent in the same commit either way. Recorded on `tokenize`'s docstring so the next
session does not "align" them.

### Knowledge revisions

None — the boundary rule in §10.3 was already right; only the implementation order was wrong.

### Open threads

Unchanged from the P4.1–P4.5 entry above.

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test` — 339 passed
- [x] `npm run build:web` — emits `dist/`
- [x] Review comments addressed
- [x] Journal entry written

---

## P4.6 — error rendering on the result cell

### Session log

1. Extended `ResultDerived` with an optional `outcome` (`stale` | `error`) so the result
   cell can paint §9 / §10.4 states without waiting on P4.7's lifecycle to invent a parallel
   channel. Zod schema and `ARCHITECTURE.md` §6 updated in the same commit.
2. `explainEngineError` / `resultCellContent` in `errors.ts` — one source for explanation
   copy and for the text `widthOf` measures, so the cell never sizes against a cached
   number while showing "Division by zero".
3. `ResultNode` renders values normally, stale values at `STALE_RESULT_OPACITY`, and errors
   as multi-word explanations (smaller glyph size so they read as messages). No bare `?`.
   `CircularReference` gets a stub explanation; naming the cycle remains P6.3.
4. Component test per §10.4 kind that P4.6 owns, plus Stale and the CircularReference stub.

### Findings

**`derived.outcome` is the paint cache for status, not a second source of truth.**
The engine still owns whether a chain is Incomplete / ErrorState; `outcome` is what the
last recompute wrote so the cell can paint before (or without) a live status lookup. P4.7
will write it; P4.6 only consumes it. Same rule as `display` itself (§6, decision #6).

### Knowledge revisions

None.

### Open threads

- **P4.7** must write `derived.outcome` on recompute (stale while dirty, error kinds from
  evaluate/validate, clear on success) or the new rendering path stays unreachable from
  the UI.
- Carry-forward: ctrl+wheel passive listener; P7.7 device fps; P6.8 palette a11y.

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test` — 358 passed
- [x] `npm run build:web` — emits `dist/`
- [x] Acceptance criteria ticked for P4.6
- [x] `ARCHITECTURE.md` §6 updated with `ResultDerived`
- [x] Journal entry written

---

## P4.6 review follow-up (PR #66)

### Session log

1. Fixed the error-mode width bug the review found: `widthOf`'s result branch now picks
   `RESULT_ERROR_FONT_SIZE` when `resultCellContent` is in error mode, and ResultNode
   imports the same constant. Layout / hit-test / snapping no longer disagree with the
   rendered glyph size. Strengthened the measure test to assert the exact width at 16,
   not merely `errorWidth > shortValueWidth`.

### Findings

**Result error font size must be decided inside `widthOf`, not only in the view.**
`ResultNode` rendered explanations at 16 while `widthOf(..., locale)` defaulted to
`numeralFontSize` (30). The cell was ~126dp too wide — more than `nodeHeight` itself.
Every other `widthOf` call site (layout, bounds, snapping, hit-test, commands) would have
inherited the same lie the first time P4.7 wrote a live error outcome.
*Misleading symptom:* relative tests (`errorWidth > shortValueWidth`) pass either way,
because the explanation is still longer than `"1"` even at the wrong size.
*Fix:* shared `RESULT_ERROR_FONT_SIZE` next to `resultCellContent`; `widthOf` selects it
from `content.mode`.

### Knowledge revisions

None.

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test`
- [x] `npm run build:web`
- [x] Review comments addressed
- [x] Journal entry written

---

## Process — cross out finished tasks, not just tick them

### Session log

1. Asked to make the "done" convention harder to miss: a task with every acceptance box
   ticked was otherwise textually identical to an open one — same heading, same node label
   in the phase's mermaid diagram — with only a fill colour (`style Pxx fill:#22A75B`) or a
   scan of every checkbox telling the two apart. Extended the existing whole-*phase*
   strikethrough convention (`~~**P0**~~` in the Status table) down to individual *tasks*:
   when every box on a task is ticked, its `### Pn.n — ...` heading and its label in any
   mermaid dependency diagram get struck through too, in the same commit as the boxes.
2. Updated `AGENTS.md`'s "Keep docs from going stale" bullet (the single source of truth
   per its own sync table) and `DEVELOPMENT_PLAN.md`'s "How to use this as an agent" step 4
   to state the rule.
3. Applied it retroactively to every already-fully-ticked task so the document doesn't
   contradict its own new rule the moment it's written (P2.1–P2.10, P3.1–P3.7, P4.1–P4.6
   headings; the P3 and P4 mermaid diagrams' matching nodes, including the already-done
   upstream P2/P3 tasks the P4 diagram references as dependencies). P4.7–P4.9 and both
   diagrams' phase-exit nodes are untouched — their boxes are still open.

### Open threads

- Mermaid's own markdown-string support (used for `**bold**`/`*italic*` in node labels)
  does not document `~~strikethrough~~` as a supported inline form. The convention was
  applied on the assumption that GitHub's mermaid renderer either honours it or degrades to
  showing the literal tildes — both are acceptable (tildes still read as "struck"), but a
  renderer that instead errors on the syntax would not be caught by `tsc`/`lint`/`test`.
  Worth a one-time visual check next time `DEVELOPMENT_PLAN.md` is viewed on GitHub.

### Closing checklist

- [x] `AGENTS.md` and `docs/DEVELOPMENT_PLAN.md` updated in the same change
- [x] Convention applied retroactively so the docs don't start out contradicting themselves
- [x] Journal entry written

---

## Process revision — diagram/header strikethrough reverted

Follows directly from the "cross out finished tasks" entry above, merged as PR #69.

### Session log

1. Told directly, after that PR merged: don't strike through mermaid diagram labels, and
   don't strike through task headings either — status stays checkbox-only in prose and
   colour-only in the diagrams (the convention as it stood before that PR).
2. Reverted `AGENTS.md`'s bullet and `DEVELOPMENT_PLAN.md`'s workflow step 4 to say so
   explicitly (colour-only diagram fill, no struck headings), and reverted every heading and
   diagram-node strikethrough the prior entry had added (P2.1–P2.10, P3.1–P3.7, P4.1–P4.6,
   and the P3/P4 diagrams), including the pre-existing whole-*phase* strikethroughs in the
   Status table and the P0/P1 phase headings (`~~**P0**~~` etc., predating both entries),
   which a first-pass blanket find/replace for `~~...~~` briefly took out along with the
   ones meant to be reverted — caught and restored by diffing against the pre-PR-69 file
   before committing.

### Knowledge revisions

**7. The per-task strikethrough convention from the previous entry is not what should ship.**
*Previously believed:* extending the whole-phase `~~**P0**~~` strikethrough convention down
to individual task headings and mermaid node labels was a good clarity improvement, worth
applying retroactively — recorded as this file's own preceding entry and shipped in PR #69.
*Now known:* rejected on direct instruction — task headings stay plain (checkboxes alone
carry status), and diagram nodes stay colour-only (no label markup change). The whole-phase
convention in the Status table is unaffected; only the task-level extension is reverted.
*What established it:* explicit user correction after the PR merged, not new evidence about
whether the mermaid markup itself renders — that question from the prior entry's Open
threads is now moot for this codebase, since nothing here calls for `~~` in a diagram label
anymore.
*Standing lesson:* "kept current alongside the acceptance-criteria boxes" (the diagrams'
own legend text, restored by this entry) was already the full spec — extending a convention
by analogy from one scope (phases) to a narrower one (tasks) still needs a check before
committing to it as house style, even when the analogy reads as a strict improvement.

### Closing checklist

- [x] `AGENTS.md` and `docs/DEVELOPMENT_PLAN.md` diffed against the pre-PR-69 revision to
      confirm no pre-existing strikethrough (Status table, P0/P1 phase headings) was lost
- [x] Journal entry written

---

## P4.7 review follow-up (PR #68)

### Session log

1. Rebased onto `origin/main` after P4.6 landed (PR #66/#67). Resolved plan/journal
   conflicts; kept main's `measure.ts` (error-font sizing) over the comment-only
   change from the pre-rebase commit.
2. `ensureResultNodeForChain` now writes `derived.outcome = { status: 'error', error }`
   on eval failure so `resultCellContent` shows the explanation rather than a blank
   cell — the review's real issue against §10.4.
3. Added a lifecycle test for `1 ÷ 0 =` asserting the error outcome shape.

### Findings

None beyond the review itself.

### Knowledge revisions

None.

### Closing checklist

- [x] Rebased onto main with P4.6
- [x] Error path populates `derived.outcome`
- [x] DivideByZero lifecycle test added
- [x] Journal entry written

---

## P4.8 — recompute on edit

### Session log

1. Added `src/engine/graph.ts`: `dirtyClosure` (P4.8 = seed only; P6.2 widens in place),
   `markChainsStale`, `recomputeChain`, `recomputeFromSeeds`. Result create/update/remove
   now lives here so P4.7's create-only path and edit-time update share one writer of
   `derived`.
2. `documentStore.applyCommand` accepts optional `{ recomputeSeeds, locale }` so mark→
   evaluate runs inside the same `produceWithPatches` turn as the mutation (§11 same-commit
   rule). `finalizeChain` / `setNodeRaw` call `recomputeFromSeeds` on the draft (nested
   recipes can't use the option for a second chain without accumulating seeds).
3. Replaced `ensureResultNodeForChain` with the graph path. Editing an input on an
   Evaluated chain updates the existing result in place; leaving Evaluated removes it
   (same "no result" rule as Incomplete create).
4. Tests: graph unit suite (including "untouched chains never evaluated" via a
   `computeChain` mock), commands integration (`1221+3=` → edit `1221→1300` → `1,303`),
   store option undo round-trip. Plan diagram: P4.8 green, P4.9 amber; status 8/9.
5. `ARCHITECTURE.md` §11 notes the `dirtyClosure` extension point.

### Findings

**Immer drafts passed into `computeChain` during `applyCommand` are revoked after the
recipe.** A test that spies `computeChain` and later reads `chain.id` from
`mock.calls` gets `Cannot perform 'get' on a proxy that has been revoked`. Capture the
id inside the mock implementation (or copy primitive fields) before the recipe ends.

**`setNodeRaw` must reflow after recompute, not before.** Result display width can change
with the new value; reflow-then-recompute leaves one frame's worth of wrong layout in the
committed document. Reflow is therefore inside the same recipe, after `recomputeFromSeeds`.

### Knowledge revisions

None — §11's single-chain half matched what we built.

### Open threads

- **P4.9** continuation (result + operator → new chain with reference) is next; depends
  on P4.7/P2.8, not on this cascade.
- Carry-forward: ctrl+wheel passive listener; P7.7 device fps; P6.8 palette a11y;
  P4 phase-exit hand check once P4.9 lands.

### Closing checklist

- [x] `npx tsc --noEmit`
- [x] `npm run lint`
- [x] `npm test` — 381 passed
- [x] `npm run build:web` — emits `dist/`
- [x] Visual: edit an input, result updates (`npm run web`) — `5÷3=1.66…` → `8÷3=2.66…` → `8÷4=2`
- [x] Acceptance criteria ticked for P4.8; mermaid node green
- [x] Journal entry written

---

## P4.8 review follow-up (PR #71)

### Session log

1. Deduplicated result deletion: exported `removeResultNodesForChain` from
   `engine/graph.ts` and deleted the twin in `commands.ts`. Equals-loss
   (`finalizeChain`) and not-Evaluated recompute now share one implementation —
   the review's only follow-up ask.

### Findings

None beyond the review itself.

### Knowledge revisions

None.

### Closing checklist

- [x] Shared `removeResultNodesForChain`
- [x] Unit test for the exported helper
- [x] Journal entry written

---

## P4.9 — Continuation (§8.7)

### Session log

1. Branched `cursor/p49-continuation` from the P4.7 branch (`cursor/p47-result-node-lifecycle`),
   which P4.9 depends on and which is not yet on `main`.
2. Replaced P2.8's result+operator no-op with `continueFromResult`: selected result + operator
   creates a new chain below-right of `R` containing `[ reference→R , ⊕ ]`, selects the
   operator so the next digit appends a fresh number, and never edits `R`.
3. Unlocked the minimum surface area references need before P6: `createReferenceNode`,
   `widthOf` for references (optional nodes map), a neutral no-hue `ReferenceNode` view,
   hit-testing, and `computeChain(..., chains)` with a live resolver that recomputes a
   result's source chain rather than trusting `derived` (§6).
4. Integration test covers the full keystroke path `10 + 5 =` → continue `×` → `2 =` →
   `30`, then editing the source `10→20` and re-evaluating the continuation yields `50`.
   Automatic cascade on edit remains P4.8 / P6.2 — resolution itself is live.
5. Ticked P4.9 in `DEVELOPMENT_PLAN.md`. Connector and identity hue deliberately deferred
   to P6.5/P6.6.

### Findings

- **`widthOf` for references needs the node map.** Measuring a reference without resolving
  its target is what the old "throw until P6" guard was avoiding. Layout/hit-test always have
  the map; a fallback to the `nodeHeight` floor keeps stray calls from throwing mid-gesture.
- **Live reference resolution must recompute the source chain, not read `ResultNode.derived`.**
  Otherwise continuation freezes a poisoned or stale cache. `computeChain` now takes an
  optional `chains` map so the resolver can walk result→source without trusting the cache.
- **Continuation offset is a design token in all but name.** `CONTINUATION_OFFSET`
  (`0.5 × nodeHeight` right, `1.5 × nodeHeight` down) was read off
  `docs/assets/linking-model.svg` proportions; revisit if real-device spacing feels wrong.

### Knowledge revisions

**8. P4.9's "Touches" list understates the unlock.**
*Previously believed (from the plan's own Touches line):* continuation is a commands/keymap
change only.
*Now known:* creating `[reference, ⊕]` forces measure, layout, hit-test, a minimal
reference view, and `computeChain` resolution in the same task — otherwise the new chain
cannot lay out, paint, or evaluate. Hue and connectors stay P6; the structural unlock does not.
*What established it:* `widthOf` threw on `reference`, `NodeLayer` returned null, and
`computeChain` called `evaluate` with no resolver — all three would have left P4.9's
acceptance criteria green only on paper.
*Standing lesson:* when a task pulls a Phase-6 type into existence early, budget the
read-path unlock (measure/render/evaluate) alongside the write path, or the feature is a
store mutation nobody can see.

### Open threads

- Automatic cascade from a source edit into a continuation chain still needs P6.2
  (transitive dirty closure). Same-chain recompute is P4.8 and is now on this branch;
  `recomputeChain` must pass `draft.chains` into `computeChain` or references
  degrade to `NotANumber` (PR #72 review).
- Neutral grey reference chrome is interim; P6.5 replaces it with identity hue.

### Closing checklist

- [x] `tsc --noEmit`, `npm run lint`, `npm test`, `npm run build:web` green
- [x] P4.9 acceptance boxes ticked
- [x] Journal entry written
---

## P4.9 review follow-up (PR #72)

### Session log

1. Merged `origin/cursor/p48-recompute-on-edit-fab4` into this branch so P4.8 and P4.9 share
   one history — the review's integration gap (#71's `recomputeChain` called `computeChain`
   without `chains`, which would make every reference resolve as `NotANumber` once both
   landed).
2. `recomputeChain` now passes `draft.chains` into `computeChain` (with a comment citing
   the review).
3. Exported `normaliseDecimal` from `evaluate.ts` and reused it in `resolveReferenceValue`'s
   number case so the two paths cannot drift.
4. Added a `CircularReference` cycle-guard test on `computeChain`.

### Findings

None beyond the review itself.

### Knowledge revisions

None — the review's diagnosis matched: optional `chains` is load-bearing once references exist,
and the dirty-set path is the call site that must not omit it.

### Closing checklist

- [x] P4.8 merged; `recomputeChain` passes `draft.chains`
- [x] `normaliseDecimal` shared
- [x] CircularReference test added
- [x] Journal entry written
---

## P4 phase review and close-out

### Session log

1. `origin/main` already had P4.1–P4.9 (PRs #65/#67/#68/#71/#72 all merged). Restarted this
   branch from `origin/main` per the merged-PR rule rather than stacking on stale history.
2. Ran the full verification suite on `main` myself: `tsc --noEmit`, `eslint .`, `jest`
   (390/390 at that point), `build:web` — all clean. The plan doc already had every P4.x
   acceptance box ticked and the phase diagram all green; only the phase exit check itself
   (never independently, live-verified) was still open.
3. Live-verified the exit check with Playwright + Chromium against the real dev server
   (`npm run web`), not type-checked only (`2026-08-03` revision 8): `1221 + 3 - 20 = 1204`;
   editing `1221 → 1300` recomputes to `1283`; `5 ÷ 0 =` renders "Division by zero"
   distinguishably; `0.1 + 0.2 = 0.3` exactly; an unbalanced paren stays `Incomplete` with no
   result node; the result cell rejects a digit press.
4. Found a real bug live: `2 × (3 + 4) =` — the exit check's own example — evaluated to `7`.
   Diagnosed and fixed in `src/keypad/keymap.ts` (see Findings); re-verified live and added
   a regression test in `keymap.test.ts`.
5. Ticked the P4 phase exit check box; flipped the status table row and the top-level status
   diagram's `P4` node to done/green.

### Findings

**Opening a paren right after a bare operator silently split the chain in two.** Typing
`2`, `×` auto-appends an operator *and* an empty placeholder number, selected and in edit
mode, so the next keystroke has somewhere to land. Pressing `(` next is a *structural* key
(`isStructural` covers operator/paren/equals), and `dispatchEditorCommand`'s guard for "a
structural key can't sensibly continue an empty in-progress number" unconditionally
discarded that placeholder and fell through to the "nothing selected" branch — which creates
a brand-new *free* paren at `ui.lastInteractionPoint`, not a continuation of the `2 ×` chain.
The result: two disconnected chains, `2 ×` (stranded `Incomplete`, silently invisible) and
`(3 + 4)` (self-contained, `= 7`). `preventFocusSteal` (Keypad.tsx's mousedown-preventDefault
fix from an earlier, unrelated bug) was not the cause here — this was the discard-guard
itself, working exactly as written, just wrong for this one case. §10.2 decision #4
("implicit multiplication only before `(`") makes `(` the one structural key that *must*
continue the chain a trailing operator started, not abandon it — none of P4.1–P4.9's own
unit tests caught this because they all build chains through the command layer directly
(`addNumberNode` / `appendOperatorAndNumber` / …), never through `dispatchEditorCommand`'s
actual keystroke-by-keystroke discard/fallthrough logic the way a real keypress sequence
does. Fixed by special-casing `paren` + `side: 'open'` in the guard: delete the placeholder,
then re-anchor the paren append on the chain's new last member (the operator) instead of
falling through to "nothing selected". Scoped narrowly to paren-open — a second bare
operator or a trailing `=` after an empty placeholder still discard-and-fall-through, which
is correct for those (there's no structural reason to continue the chain into an ambiguous
double-operator or a `=` on a still-Incomplete sequence).

### Knowledge revisions

None — §10.2's implicit-multiplication rule was already correct; the bug was purely in the
P2.8 keystroke-dispatch layer's interaction with it, not the engine or the architecture.

### Open threads

- Carried over, unresolved: ctrl+wheel passive listener (`2026-08-04` P3 close-out); P7.7
  device fps; P6.8 palette a11y.
- Whether the same discard-guard has an analogous gap for `paren-close` or a *second*
  operator immediately after a bare one was not investigated beyond the reasoning above —
  worth a deliberate look if either comes up live again, rather than assuming this fix's
  scoping covers every structural-key combination.

### Closing checklist

- [x] `tsc --noEmit`, `npm run lint`, `npm test` (391), `npm run build:web` green
- [x] Live Playwright verification against the real dev server, screenshots inspected
- [x] Bug found live, fixed, re-verified live, regression test added
- [x] P4 phase exit check ticked; status table and top-level diagram flipped to done/green
- [x] Journal entry written

## P5.1 — Serialiser

### Session log

1. Added `src/persistence/serialize.ts`: `serializeDocument` / `deserializeDocument` /
   `toSerializedDocument` / `stripDerived`. Records stay in memory; the wire shape uses
   sorted-by-id arrays for `nodes` and `chains`, recursive key sorting, a `$schema` URL
   matching §12.1, and strips every result's `derived` on write. Pretty-printed JSON
   (2-space + trailing newline) so files stay inspectable and git-diffable (decision #5).
2. `deserializeDocument` is the structural inverse only — arrays → Records, drop `$schema`.
   No zod (P5.2) and no layout re-run (P5.5); positions come back as written so the
   round-trip equality test is meaningful. Member-position-ignored-on-load remains a load-
   pipeline obligation.
3. 8 tests in `src/persistence/serialize.test.ts`: array order, derived stripped, positions
   written, `$schema`, byte-stability across Record insertion orders, round-trip equality
   against the derived-stripped original, empty document, no input mutation.
4. Reconciled the docs that disagreed with the §12.3 save sequence P5.1 cites: §12.1 sample
   no longer shows `derived`, its note and decision #6 now say stripped-on-write /
   tolerated-on-load, and `ResultNode`'s comment matches. Flipped P5.1 (and the already-done
   P4EXIT / P4.8 nodes the P5 diagram still had grey) to green in the plan.

### Findings

**§12.1 sample / decision #6 disagreed with the §12.3 save sequence before this task.**
The sample carried a `derived` block and decision #6 said "persisted as labelled cache",
while the save-sequence diagram already said "strip derived". P5.1's acceptance criteria
cite §12.3, so the write path strips; the docs were brought into line rather than the
criteria. A hand-edited file that still has `derived` remains loadable — P5.2's zod schema
already accepts it as optional.
*Misleading symptom:* reading only §12.1 or decision #6 makes "persist derived" look like
settled design; the operational write path was already the other way.

### Knowledge revisions

**1. Decision #6 does not mean `derived` is written to disk.**
*Previously believed:* derived values are persisted as a labelled cache so a freshly opened
document can paint before evaluate and so saved files show answers (decision #6 as written;
§12.1 sample; `ResultNode` comment).
*Now known:* the serialiser strips `derived` on every write (§12.3 / P5.1). In-memory paint
between keystrokes still uses it; on load the engine recomputes before the document is
ready. A file that somehow still carries `derived` is tolerated and overwritten. Decision
#6's revisit condition is now the place that would restore write-through if instant paint
from autosaved files without evaluate becomes load-bearing.
*What established it:* P5.1's acceptance criteria and the §12.3 save-sequence diagram both
already said strip; the sample and decision #6 had drifted from that. Implementing the
task forced the reconciliation.

### Open threads

- Carried over, unresolved: ctrl+wheel passive listener (`2026-08-04` P3 close-out); P7.7
  device fps; P6.8 palette a11y; paren-close / second-operator discard-guard gap from the
  P4 close-out entry.
- P5.2 must validate the on-disk *array* shape (`nodes`/`chains` as arrays, optional
  `$schema`) — today's `calcDocumentSchema` still expects in-memory `Record`s. Settle by
  giving the wire schema its own zod type (or a preprocess) rather than overloading the
  in-memory one.
- Whether pretty-print indent (2-space + trailing newline) should be frozen as a decision
  rather than an implementation detail — settle if a second writer appears (export path,
  tests that snapshot files).

### Closing checklist

- [x] `tsc --noEmit`, `npm run lint`, `npm test` (399), `npm run build:web` green
- [x] P5.1 acceptance criteria ticked; mermaid node green; P4EXIT/P48 colours corrected
- [x] Architecture §12.1 / decision #6 / `ResultNode` comment reconciled with strip-on-write
- [x] Journal entry written

---

## P5.3 — Storage adapter and native implementation

### Session log

1. Picked up P5.3 (user-directed; P5.1 is the lower-numbered peer also unblocked by P4's exit,
   but both are independent behind that gate). Read §12.2 / §12.3 in full.
2. Added `src/persistence/adapter.ts` with the §12.2 `DocumentMeta` / `StorageAdapter` surface
   plus a stub `storageAdapter` so TypeScript (no platform resolution under
   `moduleResolution: "bundler"`) still typechecks `import { storageAdapter } from './adapter'`.
   Metro/webpack substitute `adapter.native.ts` / `adapter.web.ts` at bundle time.
3. Implemented `adapter.native.ts`: documents at
   `DocumentDirectoryPath/calcmind/<id>.calcmind.json`; atomic write via `.tmp` → flush barrier →
   rotate primary to `.bak` → rename `.tmp` over primary; exactly one `.bak` generation.
4. In-memory Jest mock for `@dr.pogodin/react-native-fs` (TurboModule cannot load under Jest);
   shared behavioural contract in `adapter.sharedTests.ts` for P5.4 to reuse; native-specific
   atomicity / path / crash-mid-save tests.
5. Ticked P5.3 acceptance boxes; flipped P5 diagram nodes for completed prerequisites
   (`P4EXIT`, `P48`) and `P53` to green; status table P5 → in progress.

### Findings

**`@dr.pogodin/react-native-fs` has no `fsync`.** §12.3's write protocol is
`.tmp → fsync → rename`. The library's `writeFile` / `WriteFileOptionsT` only expose encoding and
iOS `NSFileProtectionKey` — no flush/fsync call. On Android the module's own comment says the
promise resolves only after the output stream is closed (and thus flushed); iOS uses
`NSFileManager createFileAtPath:contents:`. The native adapter treats `writeFile`'s resolved
promise as the durability barrier before `moveFile`. A crash mid-save still cannot leave a
*truncated primary* (payload lands in `.tmp` first); what we cannot guarantee without a real
`fsync` is that the `.tmp` bytes reached stable storage before a power loss. Misleading symptom
if chasing "atomic write": looking for an `fsync` export on this package and finding none.

**Platform module + shared types under RN TypeScript.** `adapter.ts` (types + stub) and
`adapter.native.ts` (runtime) coexist because tsc with `moduleResolution: "bundler"` does **not**
prefer `.native.ts`, while Metro does. A types-only `adapter.ts` would make
`import { storageAdapter } from './adapter'` fail typecheck. The stub must throw if ever executed —
that means a bundler failed to pick a platform file. `import type` from `./adapter` inside
`adapter.native.ts` is safe: Babel erases it before Metro would otherwise self-resolve.

### Knowledge revisions

None.

### Open threads

- Carried over: ctrl+wheel passive listener; P7.7 device fps; P6.8 palette a11y;
  discard-guard follow-ups for paren-close / second operator.
- P5.4 should run `defineStorageAdapterContract('web', …)` against the IndexedDB adapter with
  no Jest/webpack config change for `.web.ts` resolution — verify rather than assume (§5.1).
- P5.5 needs a way to read `.bak` when the primary is corrupt. `read()` is primary-only per
  §12.2; `bakPath(id)` is exported from `adapter.native.ts` for that. Web has no `.bak`
  (IndexedDB transactions). Decide in P5.5 whether to add an optional `readBackup?` on the
  adapter or keep path helpers native-only.

### Closing checklist

- [x] P5.3 acceptance criteria ticked; diagram node green
- [x] `tsc --noEmit`, lint, test, `build:web` (run before commit)
- [x] Journal entry written

---

## P5.3 PR review follow-up (#95)

### Session log

1. Addressed review on #95: documented the primary-absent crash window between
   `primary→.bak` and `.tmp→primary` in `atomicWrite` itself (P5.5 must treat
   missing primary like corrupt for `.bak` fallback); dropped the dead
   `unlinkIfExists(primary)` before the final rename; rejected unsafe document
   ids at the path boundary (`assertSafeDocumentId`); noted `list()`'s full-file
   read cost; extended the FS mock with `__setFailNthMove` and a regression test
   for the second-move crash window.

### Findings

**Rotate-then-rename has a primary-absent window.** After `moveFile(primary, bak)`
and before `moveFile(tmp, primary)`, neither the old nor the new document sits at
the primary path — content is at `.bak` and `.tmp`. §12.3's "old file or new
file" guarantee for the *primary path* therefore depends on load-time `.bak`
fallback treating ENOENT like corrupt JSON. That is P5.5's job; the adapter's
`read()` stays primary-only per §12.2. Misleading symptom: reading the ticked
P5.3 atomicity box literally and assuming a crash mid-save always leaves a
readable primary.

### Knowledge revisions

None.

### Open threads

- Carried over from P5.3: P5.5 must treat primary-missing as bak-fallback; web
  contract reuse of `defineStorageAdapterContract`; optional `readBackup?` vs
  native `bakPath` helpers.
- Simulator/device smoke of real `mkdir`/`moveFile` before this leaves draft
  (review process note) — still open; Jest mock cannot prove native rename
  semantics.

### Findings (continued)

**Value imports from `./adapter` inside `adapter.native.ts` recurse under Jest/Metro.**
Platform resolution prefers `adapter.native.ts` for `./adapter`, so a live import of
`assertSafeDocumentId` from `./adapter` inside the native file resolved to itself and
stack-overflowed on first access (`Object.get [as assertSafeDocumentId]` looping).
`import type` is safe (erased before resolution); shared runtime helpers must live in a
file with no `.native`/`.web` twin — here `documentId.ts`. Misleading symptom: infinite
getter recursion only under Jest, while `tsc` (no platform resolution) is green.

---

## P6.1 — Dependency graph

### Session log

1. Picked up P6.1 (#83). Plan diagram still painted the whole phase grey on "P4.8 not
   yet merged"; `main` already had P4 closed (phase exit check ticked). Started from
   `origin/main`.
2. Extended `src/engine/graph.ts` with `buildDependencyGraph`, `dependencyEdgeKey`, and
   `topologicalOrder`. Vertices = chains; an edge `A → B` exists when `B` holds a
   reference to a node in `A`. Edges live in a `Map` keyed `(sourceNodeId, referenceNodeId)`.
   `dependents` is the chain-level adjacency P6.2 will walk. `dirtyClosure` left as the
   P4.8 seed-only stub — widening it is P6.2's job, same signature.
3. Tests cover the §11 linear/diamond orders, 1→N edge keying (two consumers of one
   source stay two edges), dangling/free targets contributing nothing, and topo
   overriding vertices insertion order when the dependent is listed first.
4. Ticked P6.1; flipped P4.8 + P6.1 green and the now-unblocked P6.2/3/4/5/7 amber in
   the phase diagram; status table P5/P6 no longer "Blocked on P4".

### Findings

**Result targets resolve via `sourceChainId`, not `chainId`.** A result's producing chain
is `sourceChainId` (same rule `compute.resolveReferenceValue` already uses). Numbers and
other members use `chainId`. Mixing the two would mis-wire a result that somehow sat
outside its source chain's `members`.

**`topologicalOrder` always returns every vertex.** Kahn's algorithm emits the DAG
prefix; any leftover cycle members are appended in vertices order rather than dropped.
P6.3 will colour those chains `CircularReference` at build time — until then, callers
that only feed DAGs see a total order, and cascade code never silently loses a chain id.

### Knowledge revisions

**1. P6 was not blocked on an unmerged P4.8.**
*Previously believed (from the P6 diagram commentary as written):* the phase's one
cross-phase dependency, P4.8, was "not yet merged," so every P6 task read grey.
*Now known:* P4.8 and the rest of P4 are on `main` (phase exit check verified live).
P6.1 was ready; the diagram text was stale.
*What established it:* `docs/DEVELOPMENT_PLAN.md` P4 section all boxes ticked;
`docs/journal/2026-08-04.md` P4 close-out; `git log` on `main`.
*Standing lesson:* before treating a phase diagram's grey nodes as a gate, check the
task boxes and whether the cited dependency's own boxes are already green — colour
lags when commentary is not updated in the same commit as the merge.

### Open threads

- Carried over: ctrl+wheel passive listener; P7.7 device fps; P6.8 palette a11y;
  paren-close / second-operator discard-guard gaps from the P4 close-out.
- **P6.2** is next for the cascade: widen `dirtyClosure` with this graph. P6.3 (cycles),
  P6.4 (dangling UI), P6.5 (hue), and P6.7 (drag-result→reference) are also unblocked
  from P6.1.

### Closing checklist

- [x] `npx tsc --noEmit`, `npm run lint`, `npm test` (399), `npm run build:web` green
- [x] Acceptance criteria ticked; mermaid P6.1 green; status table updated
- [x] `ARCHITECTURE.md` §11 notes P6.1's `buildDependencyGraph` / `topologicalOrder`
- [x] Journal entry written

---

## P6.1 review follow-up (PR #94)

### Session log

1. Addressed the review's soft asks: documented that a graph edge means "B reads a
   node in A," not "the reference evaluates" (operator/paren targets still wire an
   edge; compute yields `NotANumber`). Froze `dependents` adjacency lists on return
   so the `readonly` typing is honest at runtime for the non-blocking ReadonlyMap nit.

### Findings

None beyond the review itself.

### Knowledge revisions

None.

### Open threads

Unchanged from the P6.1 entry — P6.2 next.

### Closing checklist

- [x] Review comments addressed
- [x] Tests still green
- [x] Journal entry written

---

## P6.4 — Dangling references

### Session log

1. Picked up P6.4 (#86). Depends on P6.1 + P2.9, both done. Started from `origin/main`.
2. Added `lastKnownDisplay?: string` on `ReferenceNode` (optional, no schema bump) and
   `engine/reference.ts` for display/dangling detection, delete-time stamping, and re-point
   eligibility. `chains/referenceDisplay.ts` re-exports so P4.9 call sites keep compiling.
3. `deleteNode` and `removeResultNodesForChain` now stamp consumers via
   `deleteNodesLeavingDanglingRefs` before removing the target — references stay in the
   document in `DanglingReference` (derived from missing target + stamp).
4. UI: struck-through dimmed cell on `ReferenceNode`; tap opens `DanglingRecoverySheet`
   (explain + re-point / convert); long-press gains `Unlink from parent` for references;
   re-point mode is ephemeral uiStore state, next valid value tap retargets.
5. Commands: `unlinkFromParent`, `repointReference`. Both undoable. Convert and unlink share
   one replace-with-number path.
6. Ticked P6.4; flipped diagram P6.4 green; status table notes P6.1 + P6.4 done.

### Findings

**`lastKnownDisplay` is a stamp, not a cache of live resolution.** Live references leave it
absent; it is written only when a target is about to disappear. Cleared on successful
re-point. Optional so pre-P6.4 documents and healthy links need no migration — absence means
"no stamp," not "empty value." Misleading symptom if confused with `ResultNode.derived`:
that cache is recomputed every edit; this stamp is frozen history for the orphaned cell.

**Result deletion is also an orphaning path.** Losing `=` (or leaving Evaluated) runs
`removeResultNodesForChain`, which used to `delete draft.nodes[id]` directly. Without the
same stamp helper, deleting an equals would orphan references with an empty cell and no
explanation — same §11.2 failure mode as `deleteNode`, quieter because the user did not
press Delete on the result itself.

### Knowledge revisions

None — §11.2 already specified the behaviour; this session implemented it. One design
choice recorded for the next reader: re-point is a two-step canvas mode (choose action,
then tap a value) rather than an in-sheet picker, because the canvas already owns hit-testing
and the set of valid targets is whatever is currently on screen.

### Open threads

- Carried over: ctrl+wheel passive listener; P7.7 device fps; P6.8 palette a11y;
  paren-close / second-operator discard-guard gaps from the P4 close-out.
- **P6.2** (incremental cascade) remains the natural next engine task; P6.3 / P6.5 / P6.7
  still amber from P6.1.
- Re-point UX: no visual "pick a value" chrome yet beyond cancelling on empty tap / Escape —
  worth a glance on a real canvas once P6.5 hues make candidates more obvious.

### Closing checklist

- [x] `npx tsc --noEmit`, `npm run lint`, `npm test` (446), `npm run build:web` green
- [x] Acceptance criteria ticked; mermaid P6.4 green; status table updated
- [x] `ARCHITECTURE.md` §6 / §11 / §11.2 / §14 note the stamp and recovery UI
- [x] Journal entry written

---

## P6.4 — manual verification

### Session log

1. Ran the web target and walked the §11.2 path in a real Chromium session: `7 =`
   → continue with `+` → delete the result (chain regenerates a *new* result id; the
   continuation's reference stays pointed at the old id) → dangling cell shows `7` →
   tap opens recovery sheet with both actions → convert freezes as a teal number.
2. Screenshots under `/opt/cursor/artifacts/screenshots/p64-*.webp`.

### Findings

**Deleting an Evaluated chain's result does not leave the chain result-less.**
`finalizeChain` still sees `=` and recomputes, which creates a fresh `ResultNode` with a
new id. References that pointed at the *old* result stay dangling with the stamped
display — that is the intended §11 orphaning, and it is what the manual walk hit. The
misleading symptom is "I deleted the result but another result appeared": the new cell is
a different node; the old identity is gone.

### Knowledge revisions

None.

### Open threads

Unchanged — re-point chrome still thin; P6.2 next for cascade.

### Closing checklist

- [x] Manual browser walk completed (not only tsc/test)
- [x] Journal updated
