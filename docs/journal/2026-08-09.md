# 2026-08-09

## Selected operator + operator key replaces the symbol

### Session log

1. User: with an operator cell selected, typing another operator should change
   that operator rather than appending.
2. Added `setOperatorSymbol` (undoable, recomputes the chain). Keymap's operator
   branch replaces in place when the selection is an operator. §8.7 note + unit
   tests.

### Findings

None.

### Knowledge revisions

None.

### Open threads

- Carried over from 2026-08-08: Pages source switch; PR-preview end-to-end
  re-verify; `keep_files` accumulation; native / mobile-Safari confirm of
  `inputMode="none"`; whether drag-to-link should accept numbers.

### Closing checklist

- [x] `setOperatorSymbol` + keymap replace path
- [x] Unit tests (replace + recompute; same-op no-op)
- [ ] `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build:web` green
