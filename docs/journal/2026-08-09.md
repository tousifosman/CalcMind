# 2026-08-09

## Group-mode keypad: history only, operators if result

### Session log

1. User: when a group is selected, hide all keypad keys except undo /
   redo / backspace; if the group has a result, keep operator buttons too.
2. `Keypad` reads `groupSelectedIds` and hides digits, number-editing, and
   `=`. History stays. Operator column (`÷ × − +`) stays only when
   `groupContainsResult` is true.
3. `dispatchEditorCommand` mirrors that: while a group is active, digits /
   editing / equals / parens no-op; backspace calls new `deleteGroup` (one
   undo entry for the whole set); operator continues from the group's
   result via `continueFromResult`.
4. `selectGroup` prefers a result in the chain as the primary keypad
   target so continuation does not need an extra tap after double-tap /
   Select group.
5. §8.5 / §8.6 updated. Live check follows on PR #144.

### Findings

**Select-group fixtures must go through `appendOperatorAndNumber` +
`appendEqualsNode`, not three free `add*Node` calls plus a lone
`appendEqualsNode` on the last number.** Misleading symptom: `selectGroup`
appears to ignore the result (group size 1, operators stay hidden) because
only the equals-append path created a chain, and the tapped operator was
still free.

### Knowledge revisions

None.

### Open threads

- Carried over: Pages source switch; PR-preview end-to-end re-verify;
  `keep_files` accumulation; native / mobile-Safari confirm of
  `inputMode="none"`; brief edit/slider flash on number double-tap.
- Mode strip (dismiss / Clear all / …) stays visible in group mode — the
  request named data keys; revisit if the strip should shrink too.

### Closing checklist

- [x] Group-mode keypad UI + dispatch + `deleteGroup`
- [x] `selectGroup` prefers result; §8.5 / §8.6 updated
- [x] Unit tests (Keypad / keymap / commands)
- [ ] Live browser: group without result → history only; with result →
      ops; backspace wipes chain; op continues
- [x] `./node_modules/.bin/tsc --noEmit`, `npm run lint`, `npm test`
      (profile flake re-run green), `npm run build:web` green
