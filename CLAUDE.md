@AGENTS.md

<!--
Everything shared across AI tools lives in AGENTS.md, imported above. Claude Code does not
read AGENTS.md on its own (the "it falls back to it" claim is false), so this import is
what routes it there.

An `ln -s AGENTS.md CLAUDE.md` symlink also works, but creating one on Windows needs
administrator rights or Developer Mode - the import is portable, so prefer it.

Add Claude-Code-specific guidance below this comment. Anything that applies to other tools
too belongs in AGENTS.md instead.
-->
